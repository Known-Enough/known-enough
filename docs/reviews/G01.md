# G01 — local integration checkpoint

Current status: **independent technical PASS; REVIEW pending human acceptance/integration**. The final repair evidence below supersedes the initial readiness blockers.

## Historical initial readiness review

2026-09-22, baseline `0bb8a0f` on clean `main`. B began prerequisite verification in response to the user's next-B-task request. Actual worker and independent read-only reviewer: GPT-6; exact variant/effort not exposed. This does not claim the scheduled Astra/high selection or human acceptance.

**Verdict: CHANGES_REQUESTED on A02.5; G01 BLOCKED.** B04 is the next backend implementation task, but its G01 prerequisite is not satisfied. No B04 implementation or A02.5 ownership transfer is claimed.

## Findings

1. **P1 — Fresh-room negotiation cannot start through the browser.** In [owner-screen.tsx](../../apps/web/src/owner-screen.tsx), `inputValues` requires an existing draft or confirmed inputs. B03's [local startup](../../apps/api/src/local.ts) creates neither. `values()` consequently returns null and the enabled Submit input draft button silently does nothing. Browser command builders/controls also omit `ACCEPT_CONTEXT` and `REQUEST_SOLVE`, both required by the working [HTTP negotiation test](../../tests/integration/http.test.ts). A must provide initial input creation, explicit context acceptance and a solve action, then verify the fresh-room UI/API flow without hidden API setup substituting for those actions.
2. **P1 — Live commands always bind decision revision 1.** The owner screen sets `const context = { decisionRevision: 1 }` even for live snapshots. Input confirmation increments the application's decision revision; subsequent browser commands become stale, and refresh cannot repair the constant. Bind commands to coherent current public/owner context and versions; cover confirmation and duration revisions with real browser/API tests.
3. **P2 — Local unknown outcomes lose unchanged retry.** [local-api-client.ts](../../apps/web/src/local-api-client.ts) throws generic network/JSON errors or schema errors before [sendCommand](../../apps/web/src/command-client.ts) reaches its parsing catch. The UI only retains an envelope for `UnknownTransportError`, so these outcomes offer no exact retry. Preserve the original envelope across network abort, malformed JSON and schema-invalid responses. Existing intercepted mock-transport tests do not exercise this path.

Repair owner: A / A02.5 unless explicitly released to B. A release was requested; no silence-based takeover. Follow-up independent review must inspect the changed implementation before G01 acceptance.

## Inspection and checks

Independent reviewer inspected local route selection/client, public/owner UI, command builders, contracts, application revision/consent enforcement, local startup, HTTP integration flow and prior A03.5 evidence. Source retains strict DTO parsing, explicit non-production labels, separate consent commands and matching proposal checks. No browser backend/fixture import was added. Findings 1–2 are source-level evidence, not fresh browser reproductions.

B restored Node 24.21.0/npm 11.19.0 under `/tmp/g01-runtime`, verifying the archive against the official SHA-256 manifest. The first archive extraction failed on a relative symlink; tar extraction completed successfully. No repository dependency/configuration changes.

- `npm test -- tests/integration/http.test.ts apps/web/src/command-client.test.ts`: initial sandbox run exited 1 (12 HTTP tests could not bind loopback; five client tests passed). Rerun with loopback permission exited 0: **17/17 tests passed**. This verifies existing HTTP behavior, not browser integration.
- `node --experimental-transform-types /tmp/g01-retry-probe.mts`: exit 0; three injected fetch failures through the actual `LocalApiClient` plus `sendCommand` reproduced finding 3. Network and malformed JSON produced ordinary Error; schema-invalid JSON produced ZodError; none retained `UnknownTransportError`. These are defect reproductions, not acceptance passes. Reproduce using any valid command from the server-only commands fixture and injected fetchers that throw, return `Response('{')`, or return `Response('{}')`.
- No application source or executable repository files changed. Documentation/reference checks are recorded in B's log. No new full suite or browser run is claimed; no live, cloud, authentication or transaction acceptance is granted.

## Next

Repair A02.5 findings, obtain independent follow-up review, then execute G01's real fresh-room negotiation, refusal/disclosure independence, three-approval and duration-invalidation browser checks plus `npm run check`. Human acceptance remains separate. B04 stays BLOCKED until that gate passes; its first identity/persistence slice must then stop at B04.5 for independent review.


## Final A02.5 repair review — 2026-09-22T18:16:17Z

**PASS on the exact artifact below.** Independent read-only reviewer: GPT-6; variant/effort not exposed. Implementer: B / GPT-6, same disclosure. User resumed work after the explicit A-to-B repair-release request, authorizing the bounded takeover. The reviewer inspected source, tests, exact file hashes and completed output, and found no remaining blocking findings. A02.5 and G01 remain REVIEW for human acceptance/integration. B04 is next, unclaimed pending that checkpoint acceptance.

### Findings closed

- Initial input creation now asks for explicit answers and costs using only public schedule questions. Unselected answers prevent submission. Draft review displays conditions/costs and requires explicit coverage checks; duplicate intervals produce one checkbox. Owners separately accept the full current setup and request the real solver.
- Commands bind the current public decision revision only when owner/public room, context and control versions match. Incoherent snapshots disable mutations and require explicit refresh; server version checks still enforce races after reads. Refreshed confirmation never silently reuses checked coverage.
- Unknown local mutation outcomes retain the exact envelope and prevent other commands until retry resolves. Network failure, invalid JSON and invalid schema are distinguished from structured rejection. Tests apply the mutation before dropping its response, then verify replay does not increment the owner revision twice.
- Repair-review finding: initial do-not-ask inputs could be rewritten as exception invitations by the legacy editor. The new explicit unavailable state preserves `inviteException:false`; helper and real-browser cost-only resubmission regressions cover this correction. The independent reviewer inspected both the correction and regressions.
- Live-browser finding: native `fetch` stored as an instance method threw `TypeError: Failed to execute 'fetch' on 'Window': Illegal invocation`. A `globalThis.fetch` wrapper preserves the browser receiver. The independent reviewer confirmed diagnosis and inspected the final wrapper. Diagnostic logging was removed; the final browser tests exercise native fetch directly.

### Fresh evidence

Runtime: pinned Node 24.21.0/npm 11.19.0 under `/tmp/g01-runtime`; Playwright Chromium build 1243 under `/tmp/g01-playwright`; required Ubuntu shared libraries downloaded/extracted under `/tmp/g01-browser-libs` without system installation. Reference hashes and lockfile preserved.

```sh
PATH=/tmp/g01-runtime/node-v24.21.0-linux-x64/bin:$PATH \
PLAYWRIGHT_BROWSERS_PATH=/tmp/g01-playwright \
LD_LIBRARY_PATH=/tmp/g01-browser-libs/usr/lib/x86_64-linux-gnu \
npm run check
```

| Check | Actual result |
| --- | --- |
| Focused local-client/command/owner helper unit tests | Exit 0, 18/18 |
| Focused typecheck | Exit 0 |
| Real browser suite `tests/e2e/local-negotiation.spec.ts --workers=1` | Exit 0, 7/7; `/tmp/g01-browser-check.log` |
| Full `npm run check` | Exit 0; 176 unit/integration, 36 browser tests; references 7/7, planning 15/15, lint/boundaries, typecheck, build/bundle scan; `/tmp/g01-full-check.log` |
| Independent final review | PASS on manifest below and completed logs |

The browser suite launches the actual B03 local startup with empty owners and an exclusive listener. Input submission, confirmation, setup acceptance, solve, exception/disclosure decisions and all three final approvals happen through UI controls. Separate refusal reaches NO_AGREEMENT without private disclosure. Organizer-only duration revision uses the real HTTP API; browser controls/receipts and server snapshots verify superseded grants, cleared approvals/proposal/coverage, and later commands beyond revision 1. This does not claim an organizer editor exists in the UI. Additional scenarios verify three unknown-outcome types, explicit no-invitation preservation and mismatched-read blocking.

Earlier failed runs are not passing evidence: initial browser launch lacked shared libraries; subsequent live runs exposed the native-fetch defect (one full attempt, diagnostic attempts and narrow reviewer escalation); the implementation/review exposed and fixed no-invitation preservation. The independent reviewer temporarily hit a usage limit, then resumed after the user resumed work and completed exact-artifact review. No unresolved finding was silently waived.

### Exact artifact

Base: `0bb8a0f`, uncommitted repair on `main`. These seven files comprise the implementation/test artifact, including new files. UTF-8 manifest below, sorted by path, one newline after each line, has SHA-256 `e9912fd6237ac889fea63c3eb0939ad9857e0e1e16d1e73f0cc89dda957e9406`. The independent reviewer verified every hash. Later source/test changes require follow-up review.

```text
be9ab9dcf59db5f81f880e3ffa6c56c26d736664cf0703b6ebbcbf4bf6108a5a  apps/web/src/command-client.ts
ead42c4216a9ed97f440f33b8c08903990e85fbaec2660fe272a1973da2936d5  apps/web/src/initial-input-form.tsx
fc1c8e76506fa102bea52112f0e17632e2264c08a3559ce0aaf04ac8bc103730  apps/web/src/local-api-client.test.ts
c97848ad825ae22c3631bb2c482689a5cea267fc7bf108536bcd3ffac1528332  apps/web/src/local-api-client.ts
8ceeac37b37c51e296711bcda0bbd0b4da3b2ab4a873c90a91972a37ff647bed  apps/web/src/owner-mock-adapter.test.ts
c49ae81fd60dc10baf903be9c67aac7eb259bade9f2e2aa356815deb3365cdd3  apps/web/src/owner-screen.tsx
cc752328630a7dee7e2376088f8a5cdf939dd8876adaa78a00591e58ca793b1c  tests/e2e/local-negotiation.spec.ts
```

Human acceptance, integration/publication, real authentication, durable persistence, cloud races and deployment remain separate. No external action was performed. B04 must retain its first-slice/B04.5 review boundary after G01 acceptance.
