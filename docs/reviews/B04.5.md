# B04.5 — midpoint identity, transaction and IAM review

Current verdict: **CHANGES_REQUESTED** on `b91ff76`; R5b encoded-byte reservation and R6 safety-counter accounting remain open. R1/R2a/R2b/R3/R4 and implemented history-slot admission are closed for the local midpoint. See the second follow-up below; earlier findings remain historical evidence.

2026-09-23T09:36:29Z. **CHANGES_REQUESTED** on committed `8e6d1c4103dc326acaf52a8c4ef3619642f68ae2..2df501d9a9c97b83a0f45fe9419c5e6c5e05b293`. Independent reviewer: B / Codex GPT-6; exact variant/effort is not exposed, so the scheduled Astra/high assignment is not claimed. Implementation owner: A / Codex GPT-6, paused throughout this sequential review. No implementation files were edited.

The initial Cognito implementation has no demonstrated authentication bypass in this review. The proposed persistence/IAM approach is not approved for expansion until R1–R4 below are addressed and the changed artifact receives follow-up review. This is a midpoint design/slice review, not B04 completion or G02 acceptance.

## Findings

### R1 — P2: proposed IAM actions do not authorize DynamoDB transactions

Location: [infra/README.md:26](../../infra/README.md). The proposal grants only `dynamodb:TransactGetItems` and `dynamodb:TransactWriteItems`. DynamoDB transaction authorization uses the underlying item actions, so this policy does not establish the permissions needed for the proposed reads and writes. AWS documents `GetItem`, `PutItem`, `UpdateItem`, `DeleteItem`, and `ConditionCheckItem` as applicable actions, with `dynamodb:EnclosingOperation` for restricting them to transactions. [AWS transaction IAM documentation](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/transaction-apis-iam.html).

Correction owner A: replace the proposed action list with the minimum underlying actions actually used by STATE/GUARD operations, one exact table ARN, and an enclosing-operation restriction. Define the `LeadingKeys` condition using the required `ForAllValues` modifier and appropriate pattern operator for `ROOM#*`; this prefix limits the storage namespace, not individual caller room membership. Include allow/deny policy cases for both transactional operations, direct item access, other tables and keys outside the prefix. [AWS condition-key documentation](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/specifying-conditions.html). No deployed policy or IAM simulation was available; this is a verified documentation/design error.

### R2 — P2: the storage proposal has no safe size/history or decoding strategy

Location: [infra/README.md:15](../../infra/README.md), [infra/README.md:22](../../infra/README.md), and [RoomRecord](../../packages/application/src/types.ts). The two-item design stores the entire room in STATE while `execute` appends a replay body/result for every new authorized valid command key, including application rejections. Agreement and permission histories also grow. The proposal acknowledges the 400 KB item limit but supplies no bounded representation or lifecycle. Repeated valid requests can therefore grow STATE until later writes, including permission revocation, fail. Rejecting an oversized write alone does not preserve a usable consent lifecycle. A schema-version field is proposed without defining runtime decoding, migration or rejection of corrupt/unknown versions.

Correction owner A: choose and document a concrete storage/history/replay strategy before implementation. Define byte/count limits, idempotency retention semantics, preservation of agreement/consent evidence, and how withdrawal/revocation/expiry remain possible at capacity. Define a validating private-record codec, key/room binding, missing or mismatched STATE/GUARD handling, and unknown schema-version behavior. This may require a bounded room lifecycle or separating retained records; do not silently prune evidence or change replay guarantees. Required adapter tests must cover near-limit operations, safe revocation, duplicate replay and malformed persisted rows. This is an unresolved design gate, not a reproduced DynamoDB failure. [AWS transaction limits](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/transaction-apis.html).

### R3 — P2: exhausted storage retries would become a non-retryable client error

Location: [infra/README.md:19](../../infra/README.md), [applicationError](../../apps/api/src/index.ts), and [HTTP catch](../../apps/api/src/index.ts). The proposal promises a retryable server error after bounded transaction retries, but the shared production/local handler maps every non-ApplicationError to `422 INVALID_COMMAND`. The client treats this valid rejection as a resolved outcome and loses the unchanged-request retry path. This is especially misleading if a queued mutation committed before subsequent worker/storage work failed.

Independent synthetic loopback probe supplied a `getPublicSnapshot` that throws `Error('synthetic storage retry exhausted')` to the shared handler through `createLocalApiHandler`; it returned exactly `422 INVALID_COMMAND`. This confirms the mapping, not a real DynamoDB outage.

Correction owner A: specify and implement a redacted server/transport error path for contention exhaustion, storage outages and unknown commit outcomes; coordinate shared contracts if adding a result code. Preserve the original command envelope/key for uncertain outcomes. Classify retryable transaction conflicts separately from validation/size failures; an application stale-context result must remain a semantic rejection, without rewriting expected versions. Add a handler regression and a committed-but-response-lost/retry regression with the real adapter once available.

### R4 — P2: signed-token tests bypass the production resolver and authenticated HTTP composition

Location: [cognito.test.ts:15](../../tests/integration/cognito.test.ts), [cognito.test.ts:37](../../tests/integration/cognito.test.ts), [cognito.test.ts:81](../../tests/integration/cognito.test.ts), and [http.test.ts:79](../../tests/integration/http.test.ts). Valid and invalid signed JWT tests call a separately configured `testVerifier` and then the claim mapper. The actual resolver is exercised only with malformed headers that return before verification. The production HTTP test covers missing bearer authentication and CORS, but no valid signed participant/display request. These tests can pass if the production verifier's issuer/client/token-use configuration or its call wiring regresses.

Correction owner A: feed local signed tokens and controlled JWKS through the actual resolver and `createCognitoApiHandler`, preserving real library signature verification. Cover valid participant owner access, subject/owner substitution, display public-only and cross-room scope, membership removal before cached replay, and wrong issuer/client/use/expiry/signature. Verify unauthenticated or unauthorized malformed bodies never reach detailed parsing, and auth failures emit no token/claim diagnostics. A controlled JWKS transport/cache seam is preferable to replacing verification with an always-successful identity stub. Local tests remain distinct from Cognito service acceptance.

## Inspected boundaries and design constraints

- `createCognitoIdentityResolver` uses the configured pool, access-token use, both explicit client IDs and zero expiry grace; it maps only verified `sub`. Display mapping requires the display client plus exactly one valid prefixed room group. Participant tokens do not become display credentials merely because of groups. Errors expose no verifier details.
- The production handler has no mock-header fallback. Except for preflight, authentication precedes URL routing and body parsing; room authorization precedes command parsing and the application authorizes again before replay lookup. CORS reflects only exact configured origins; it is not the authorization mechanism. `no-store` remains on JSON responses. Diagnostic changes remove stable subject/roster identifiers and retain bounded actor kinds, validated command types and counts; no token or private values were added.
- Admin-only group management and separate client configuration are deployment assumptions, not proven by a signed group claim. The documented proposed 15-minute token lifetime/revocation delay still needs an actual configured bound and G02 acceptance. Dedicated display credential provisioning, client sign-up/auth flows, group administration and lack of participant memberships must be verified before live use; no user-pool infrastructure exists here.
- Transactional STATE/GUARD reads and a conditional GUARD update alongside STATE replacement are a reasonable optimistic concurrency outline. All writers must obey it; discarded callback attempts must never leak results or partial mutations. Callbacks inspect current membership and expected versions again on retry. Reads can mutate state through expiry sweeps/readiness changes and must participate in the same commit discipline.
- The injected main solver runs outside the repository transaction, but the claim that all solver work is outside it is too broad: `offerConcessions` performs bounded synchronous `solveDecision` calls inside the result-application callback. No external I/O was found there; repeated callback execution may consume IDs and repeat bounded computation. Record this accurately and keep callback side effects constrained.
- Before G02, inspect the final adapter, actual IAM policy, membership/invite redemption and replay, codec, oversize behavior, authorization before cache, concurrent approval/revocation, stale revisions/jobs, commit ambiguity, and expiry under real adapter races. This first slice implements none of those live storage/invite checks.

## Checks and limitations

Commands actually run independently under `/tmp/a05-runtime`:

- `PATH=/tmp/a05-runtime/bin:$PATH npm test -- tests/integration/cognito.test.ts tests/integration/http.test.ts` — exit 0, **19/19 tests, 2/2 files**. The same focused command was repeated once to capture its untruncated result; both executions passed.
- `PATH=/tmp/a05-runtime/bin:$PATH npm run typecheck` — exit 0.
- `PATH=/tmp/a05-runtime/bin:$PATH node --experimental-transform-types --input-type=module` with the inline synthetic storage-error loopback probe described in R3 — exit 0; observed `422 INVALID_COMMAND`.

Documentation checks also passed: `npm run check:references` (7/7), `npm run check:planning` (15/15), `git diff --check`, 74 local Markdown targets, and B04/B04.5 task/board consistency.

The implementation log reports the full pinned check passed **187 unit/integration and 41 browser tests** on this slice. The reviewer did not rerun that suite. No live Cognito, DynamoDB Local/AWS, IAM simulator, cloud configuration, deployment, billing or real-data checks were performed. Read commands initially hit the environment's bubblewrap mount error; approved escalation enabled local inspection/checks. No approval rejection occurred. Missing guessed `ports.ts`/`model.ts` paths were resolved by reading the actual `types.ts`; no scope was skipped.

Requirements inspected: B04/B04.5 tickets, current agent workflow/board, security and application contracts, relevant architecture identity/transaction sections, implementation log/handoff, changed API/design/dependency/test files, and application/repository callback and record definitions. External verification used the linked AWS primary documentation only.

## Handoff

B04.5 remains **REVIEW / CHANGES_REQUESTED**; B04 remains **PAUSED at this checkpoint**. The review claim is finished. Owner A may take the next sequential bounded correction task for R1–R4, recording its changed files and checks; do not expand the unreviewed DynamoDB/IAM approach before follow-up review. Later B04 changes require G02 review regardless of this midpoint outcome. Human acceptance and external publication are not granted.

## Exact committed artifact

All 15 changed files below were read from the named committed head (tracking files have subsequent review-only edits). UTF-8 manifest in sorted path order, two spaces between hash/path and one newline per entry; SHA-256 `5f4c1d1953c29571368979d1cd6a225c728372c90d65b93100cb789dd5dddeac`.

```text
a7cbc4ff5c852ec022b29cc4a18a61c9b0c7f373f6494cb1c1ff5b482ee15650  apps/api/README.md
3ee78ea20406d50166da50e65359d6f42d1fc14083c273fb05b7d436e84c991a  apps/api/package.json
3641232f619073301c990502e996c4c5367ad0ad20a1a620faf0ba740b6da7b7  apps/api/src/cognito.ts
a284863a3c71978955f1ae11e7bb4cdad3e43591c3091e5689d00169cd1e6f6e  apps/api/src/http.ts
10922c92d5b11d887d8431f3ae3e4bf5a345c0aab95e70dae8b64630469c5802  apps/api/src/index.ts
5730ea41ac87933cc02f6fe064ad909b0cff0ad56cae4a93a69afd7732f2541a  docs/handoff-A.md
ae006981f69c3516d4ba7d078a4bb97afb16fb2cd473e63059fb8f83a6a5909e  docs/task-board.md
5161f1de14fbc6c4c519878670cab6077daf2b69a6557fc518ab48a03df6ec65  docs/tasks/B04.5.md
79dcd4b0744f3e1a356088f7bf37c26c63764bbc7810c78c830e65e71697036f  docs/tasks/B04.md
d7edf1f059437a4589c8f7dedff023fed38e234bbbd228e448b9b713c9a24e31  docs/work-log-A.md
aab301b24cb1a0bf4d4ca6fabba0808cdfa2d16bf97e0f01266e0c1b3fe519a1  infra/README.md
c3ebddd74cfd79140c7adb28f37ce78632e17d817cf810a611b9868e7c5fe5d8  package-lock.json
7a6d02e44b4fcac5543087663303a758dbb97a8d65d8f9ba388ae5e34e4ede48  tests/integration/README.md
05dc6f2ab12ead3c6fbe8e8ee2db93e73553aac12af1036e19fee7264f63069b  tests/integration/cognito.test.ts
a4fa4f45fb87dabac8ad52a93e8aabced52556902905de9b57e87ec8675b1f3b  tests/integration/http.test.ts
```

## Follow-up review — 2026-09-23T10:13:06Z

**CHANGES_REQUESTED** on committed `2df501d9a9c97b83a0f45fe9419c5e6c5e05b293..35d57e7cba22bd8fa95c0a528982ef7c83c3dcf5`. Actual independent reviewer: B / Codex GPT-6; exact variant/effort not exposed, scheduled Astra/high not claimed. Implementation owner A remained paused. R1, R3 and R4 are closed for this local midpoint artifact. R2 remains open with narrower findings R2a/R2b below; R5 identifies a regression introduced by the history cap. No implementation edits, commits, publication or cloud activity were performed.

### Findings closed

- **R1 — closed for the design.** The policy example now uses only `GetItem`, `PutItem` and `UpdateItem` on one exact table ARN, restricts their enclosing operation to the two transaction APIs, and applies `ForAllValues:StringLike` to the `ROOM#*` leading-key namespace. This matches the underlying operations in the proposed STATE/GUARD/REPLAY transactions. Creation uses conditional Put; no separate ConditionCheck action is proposed. The documented allow/deny outcomes are coherent assuming this is the complete role grant set. The prefix is correctly described as a storage namespace boundary, not per-user authorization. AWS policy simulation/deployment remains unrun; the final adapter/policy must still be checked at G02.
- **R3 — closed for implemented error/transport behavior.** The shared handler now maps unexpected failures to strict redacted `503 RETRYABLE_SERVER_ERROR`, preserving the parsed request ID. The client converts this into `UnknownTransportError` with the unchanged envelope. Tests cover an applied mutation followed by a thrown error, then an unchanged HTTP retry that reuses its existing receipt. This demonstrates local commit-before-response-loss behavior using the in-memory repository, not DynamoDB commit ambiguity or races. Semantic client rejections remain distinct. R2a concerns using this error for a different, known-no-commit capacity outcome.
- **R4 — closed for local verification.** Signed-token tests now invoke the actual Cognito resolver and real library verifier with controlled local JWKS. HTTP coverage uses the same internal production composition and tests owner binding, a forged owner field, display public-only/cross-room denial, missing/expired/wrong-issuer/wrong-client/ID/wrong-signature credentials, authorization before malformed-body parsing, and current membership before replay. Diagnostics are checked for token/private canaries. The cache seam is only an internal source export: package `exports` exposes the entrypoint, which omits the seam; the public handler does not accept a cache argument or read one from options. No HTTP input selects trusted keys, and no test key is imported into production modules. Live Cognito configuration remains unverified.

### R2a — P2: known capacity rejection would lock the client out of safety actions

Locations: [infra/README.md:19,21](../../infra/README.md), [command-client.ts:104–113](../../apps/web/src/command-client.ts), [owner-screen.tsx:110,126–127](../../apps/web/src/owner-screen.tsx).

The proposal returns retryable 503 for a new ordinary request after all 4,096 ordinary receipts have been used, and for a normal write exceeding 352 KiB. It explicitly knows these requests did not commit. The client correctly treats this 503 as an uncertain outcome, retains its exact command and disables other commands (including withdrawal) while awaiting resolution. An exact retry at permanent ordinary capacity returns the same 503 forever. At byte capacity, the proposal expects a revocation/expiry to free room, yet the affected browser's other commands are disabled. The backend's reserved safety allowance therefore does not make those actions usable in this client flow. This is a design/source incompatibility; no DynamoDB adapter exists to reproduce it end to end.

Correction owner A: return an explicit known-no-commit capacity rejection, such as the existing `ROOM_CAPACITY_REACHED`, for deterministic admission/size rejection after authorization; retain 503 only for transient/uncertain storage outcomes. Define receipt/no-receipt and exact-retry semantics for this rejection without exceeding the cap or consuming the safety reserve. Add a client regression that receives the capacity response and can then submit a withdrawal/safety action; retain the unknown-outcome retry regression. Do not remove exact-envelope protection for genuinely uncertain commits.

### R2b — P2: both absent storage rows must preserve the normal missing-room result

Location: [infra/README.md:22](../../infra/README.md). The new codec design classifies both STATE and GUARD missing as a retryable server failure, alongside one missing row or malformed data. Both absent is the normal nonexistent-room state. Existing application authorization returns `404 NOT_FOUND` for a missing room and for an existing room outside the caller's membership/scope. Implementing the proposal literally would instead return 503 for nonexistent rooms and 404 for existing unauthorized rooms, making existence distinguishable and converting normal absence into an outage.

Correction owner A: return null to the authorized application transition when both rows are absent so the existing non-enumerating NOT_FOUND behavior applies. Keep one missing row, inconsistent keys/schema or corrupt state as fail-closed storage errors. An orphan replay receipt must not become an authorized replay when STATE/GUARD are absent. Add adapter/HTTP cases comparing nonexistent and unauthorized-existing room responses once the adapter is built. This is a design finding, not a claim that the unimplemented codec currently leaks data.

### R5 — P2: history limits can prevent an owner from declining an open offer

Locations: [application index.ts:369–395](../../packages/application/src/index.ts) and [offerConcessions:552–577](../../packages/application/src/index.ts). The new exception/disclosure history cap runs before recording either ALLOW or DECLINE. Offer generation does not reserve history capacity for outstanding responses. Once an owner has 32 retained exception receipts, an offered exception cannot be declined: the transition throws `ROOM_CAPACITY_REACHED`, the working-state rollback restores the offer, and the room stays in PRIVATE_REVIEW. The current new regression tests only ALLOW rejection and revocation of an existing grant, so they miss this behavior. The same unconditional cap guards disclosure decline; the proposed post-4,096 reserve also excludes both decline commands.

Independent probe: use the real application/in-memory repository and fixture to reach a pending Nina offer, seed 32 synthetically retained DECLINED exception receipts (using the same bounded-history setup technique as the committed regression), then send the matching DECLINE command. Observed `ROOM_CAPACITY_REACHED`, one pending offer, and PRIVATE_REVIEW. The inline probe exited 0 after asserting that defect. This is a locally reproduced application defect; no real participant data or DynamoDB was used.

Correction owner A: make pending refusal possible at capacity. Reserve enough history/replay space when issuing offers/previews, or use another bounded design that can record a decline without deleting evidence or stranding a prompt. Account for every pending response, including multiple overlapping exception offers for one owner and independent disclosure refusal, and revise safety-reserve accounting if needed. Do not create a prompt whose decline cannot be committed. Add full/near-capacity offer-generation and DECLINE regressions, including confirmation that exception refusal ends the round and disclosure refusal preserves an otherwise valid exception.

### Persistence design assessment

Separate REPLAY rows and canonical SHA-256 key/body digests address STATE growth from full private command bodies. The proposed transient zero/one replay model is implementable with an explicit transaction option and recomputed authorized key; those repository options and the DynamoDB adapter are still future work. Retry attempts must reconstruct a fresh working record, authorize current membership, discard unsuccessful mutations/results and condition every write on GUARD. Callback reads can sweep expiry and need the same commit discipline. Main asynchronous solving remains outside transactions; bounded synchronous concession search still runs inside a callback, as now acknowledged.

The proposed 320 successful safety receipts exceed the stated conservative count of 192 permission grants plus 64 agreement withdrawals plus at most two approvals on a partial proposal (258). Normal commands cease after the ordinary allowance, so they cannot replenish those safety targets. This calculation does not cover the outstanding decline problem in R5. The 352/360 KiB STATE ceilings and 8 KiB REPLAY ceiling are below service item limits; revocations/expiry mainly change bounded statuses/counters or remove current state, but the future codec must measure actual encoded bytes and test the 8 KiB safety headroom across all allowed transitions. Immutable histories can reach byte capacity before count caps, so deterministic capacity handling must be coherent independently of those counts.

Strict nested allowlists, exact row keys/room IDs, explicit schema versions and reviewed migration are specified, with the absent-pair correction in R2b required. No codec, byte-budget enforcement, ordinary/safety counter enforcement, or transaction-race implementation exists yet. Those remain implementation/G02 evidence requirements; this review does not mistake the proposal for working persistence.

### Fresh checks and limits

Independently ran:

```sh
PATH=/tmp/a05-runtime/bin:$PATH npm test -- tests/integration/cognito.test.ts tests/integration/http.test.ts tests/integration/application.test.ts packages/application/src/application.test.ts packages/contracts/src/contracts.test.ts apps/web/src/command-client.test.ts apps/web/src/local-api-client.test.ts
PATH=/tmp/a05-runtime/bin:$PATH npm run typecheck
```

Results: **116/116 tests across 7/7 files passed; typecheck exit 0**. The inline Node actual-application capacity/decline probe above exited 0 and reproduced R5. Source/diff review included the moved HTTP core, entrypoints and dependency exports, JWT fixture/resolver/HTTP coverage, hash/replay and cap transitions, result schemas, command transport and error tests, and the infrastructure design. Current source remained at `35d57e7` throughout.

The implementer reports a full pinned check of **195 unit/integration and 41 browser tests**, plus references/planning/lint/typecheck/build. The reviewer did not rerun that suite; focused checks and the additional probe were sufficient to identify the remaining issues. No live Cognito, DynamoDB Local/AWS, IAM simulation or cloud operations were run. No deployed resource, revoked-token enforcement, persistent schema/race or production readiness is certified.

Documentation validation passed: reference hashes 7/7, planning checks 15/15, local Markdown targets, task/board consistency and `git diff --check`.

### Follow-up handoff

B04.5 is **REVIEW / CHANGES_REQUESTED**. The follow-up claim is finished; B04 remains PAUSED at the gate. Owner A may take the next bounded corrections for R2a/R2b/R5, then return the changed artifact for independent review before expanding the DynamoDB approach. Preserve the R1/R3/R4 closures and all historical evidence. Human acceptance and publication remain separate.

### Follow-up exact artifact

Committed 23-file delta, sorted paths, two spaces between hash/path and one trailing newline per entry. Manifest SHA-256 `fa3acb0b94ed52ecc9f493b63330f203e46af61dd474db61a55dbaf115c6ce08`. Reviewer tracking edits after the commit are outside this implementation artifact.

```text
6a3e81a4eac59b67c3c458ef3289ded59a5c6b1efae25cab92a6a8604c88ddf9  apps/api/README.md
30198494f2172ec5e0db0e779a122e6d0447eff5c60c3acc4b6a21f6a9650092  apps/api/src/cognito.ts
a452f2c57ff2a4fe3a7e917f3526f67f6314a2f68b6321496783192070c37782  apps/api/src/http-core.ts
1ca53cdd2e652e4d9336c597c97786aa26e8afd6bb53c23fc88183a6ff142bff  apps/api/src/index.ts
474624c6d0c6a852d414928dad669bb6ad99e876ebaffb2c56c2c7638be65490  apps/web/src/command-client.test.ts
35ba97161bfa7437b428a28371c9e61c73858047fb00c3e32de2b4bd811b4596  apps/web/src/command-client.ts
f124f102786ec61127a79f3ae5ad1790e90284e4002b4fd296334ceb30868d63  apps/web/src/local-api-client.test.ts
a72a78d1bbe7582cbe43ef2896f5c843111e3c7530b1905c13036ccab3aee6b2  docs/handoff-A.md
9536969fcd5757bf8d8462bfcf34f6285c9776c8e0426c7fd6bc077be82153c0  docs/reviews/B04.5.md
11b2fc23dc905ebacfd1fb165514127d8bb3df4aae1df747779a648e60b39ac4  docs/task-board.md
ad06c5041a5efc7941301d4659a30574221d040a7fcb60ffd714b0f41e4dd1bd  docs/tasks/B04.5.md
ef052eb522bdf1c3d04b26b6b91d733e14081d821d12a78e503e2cccc97c6583  docs/tasks/B04.md
0502ce22db214397c375aa38c44b2209cee943194cec3cc5d68ae7e709b93926  docs/work-log-A.md
2d45cc8f2608744a8f62d94e78b743787d867a8756e6564ee5ba9cc6d86e92d0  docs/work-log-B.md
a1dc05e75a631a5e0c46c4f7661e01d9d785a743ab589802467556d3d6d3a3a5  infra/README.md
0e01072b0763321db1f512fcfef2e20e72c9c7c69aaa7f092da575c710f5b762  packages/application/src/index.ts
a1fa7282afdad5807dbd4cdd99eaf0b9dde31799279e5b228cade05372c5f5c4  packages/application/src/types.ts
689504475ec14793c8887857196445bfc4c2422d7f1cd4583684d659a5fa62d7  packages/contracts/src/contracts.test.ts
bf023b12962b4dbb2f367a9a2ceec3312fcef3645b36f1139f3107784a59ac9c  packages/contracts/src/index.ts
846ea9a1c45bb75de9fc33eb3b1d5a89c722620f9a5fa5a3ca0a2fe16916ff3f  tests/integration/application.test.ts
dcf48b6a2e38aad9d82baa3dc98c4383e6ea997a886e30734fa38d8bfe4e72fb  tests/integration/cognito-fixtures.ts
8f194b378fecdbe1b93bd40c8596a3abe62379dca808236f538e90c9dabdc8eb  tests/integration/cognito.test.ts
716a07f769442e59edc89902a3e837dfe5ddb25d498f50e7262ffe04922618e3  tests/integration/http.test.ts
```

## Second follow-up — 2026-09-23T10:44:51Z

**CHANGES_REQUESTED** on implementation/design range `35d57e7cba22bd8fa95c0a528982ef7c83c3dcf5..b91ff76` (status/handoff head `f2a8bf606932a6fa9f451b9331aecc6a810c90da`). Actual reviewer: B / Codex GPT-6, exact variant/effort unexposed; scheduled Astra/high not claimed. Owner A remained paused. Existing uncommitted reviewer evidence was preserved. No implementation file was edited.

R2a and R2b are closed for the proposed persistence behavior; the implemented history-slot portion of R5 is closed. Two precise design gaps remain below: encoded-size reservation for pending responses (R5b) and an explicit safety-counter invariant (R6). Previous R1/R3/R4 closures remain valid; this does not reopen their implementation.

### Corrections verified

- **R2a closed:** deterministic admission/size failure now specifies `409 ROOM_CAPACITY_REACHED`, no mutation/receipt/counter write, and explicitly permits an independent safety command. Existing authorized replay precedes quota evaluation. The transport regression receives the 409 and sends WITHDRAW_APPROVAL next; source inspection confirms the owner screen sets its retry lock only for UnknownTransportError. Transient or uncertain 503 still preserves the exact envelope. The regression is at transport level, not a new browser interaction test; the future adapter still needs to implement these results.
- **R2b closed:** both STATE/GUARD absent now supply null to the application, preserving normal non-enumerating NOT_FOUND; partial/corrupt storage remains an error. Orphan replay is not surfaced, and room-ID non-reuse is specified. Actual codec and absent-row HTTP tests remain future adapter requirements.
- **R5 history admission closed:** `canReservePermissionResponses` counts each pending/new exception offer and reserves one current-context disclosure response per owner. It rejects candidates without capacity before issuing prompts, including overlapping conditions. A preview or completed current-context disclosure response shares that one slot; a disclosure decision does not revoke the exception. Regressions cover full exception/disclosure histories, insufficient slots for overlapping offers, final-slot exception decline ending the round, and final-slot disclosure decline preserving the active exception. These are actual local application behaviors, not DynamoDB quota enforcement.

### R5b — P2: pending response reservations omit encoded STATE bytes

Location: [infra/README.md:21–22](../../infra/README.md), with the response shapes in [application index.ts:369–395](../../packages/application/src/index.ts). The design reserves history entries and permission-decision replay receipts, but normal transitions still stop at 352 KiB; only safety/expiry/withdrawal updates are explicitly assigned the 360 KiB ceiling. DECIDE_EXCEPTION and DECIDE_DISCLOSURE are a separate permission category. A single pending exception can be stored just below the normal ceiling; declining it replaces the offer with a grant record containing an additional status field and can exceed that ceiling. A disclosure response also adds a grant wrapper around its stored preview. The byte rule would reject that already-issued prompt's response with 409, leaving it unanswered despite sufficient history and receipt slots. Arbitrary ordinary writes must also be prevented from consuming bytes promised to outstanding decisions.

Correction owner A: specify an encoded-byte reservation for every outstanding permitted response, including overlapping exception offers and the possible disclosure preview/decision. Either reserve the conservative worst-case response growth during offer admission and preserve it on every subsequent ordinary write, or define a separate bounded response/safety byte budget with a proved combined bound. Explicitly classify pending ALLOW and DECLINE transitions under that budget. Include JSON/key/wrapper/ID/counter growth in the bound; an assertion that every response can be recorded is insufficient without this accounting. Require near-limit tests that issue a prompt, perform other admissible writes, then commit its refusal without losing retained evidence. This is a verified design inconsistency, not a reproduced DynamoDB overflow.

### R6 — P2: 320 is not a bound on lifetime successful withdrawals

Location: [infra/README.md:19–20](../../infra/README.md); relevant existing transition: [application index.ts:414–426](../../packages/application/src/index.ts). The design calls for a successful-safety-action counter and a hard 320 ceiling while explicitly saying that the withdrawal-pattern bound is unproven. It does not unambiguously specify that successful safety actions paid from ordinary quota leave the reserved counter untouched. Partial-plan ACCEPT_PROPOSAL/WITHDRAW_APPROVAL cycles do not append agreement history and can exceed 320 before 4,096 ordinary receipts are used. Counting those successes against a lifetime 320 safety ceiling can leave no reserved withdrawal capacity when ordinary quota later fills.

Independent probe used the actual application, fixed synthetic clock, InMemoryRoomRepository and TeamTable fixture. It submitted/confirmed all owners, accepted setup, requested the real solver, allowed Nina's scoped exception and ran the resulting job. It then sent 321 successive Maya-only ACCEPT_PROPOSAL/WITHDRAW_APPROVAL pairs using current versions and new keys. Every command succeeded. Observed **321 successful withdrawals, 0 agreement-history entries, 653 total replay rows, status PROPOSED**. This proves agreement-history limits do not bound lifetime partial-plan withdrawals; it does not exercise a DynamoDB counter, which remains unimplemented.

Correction owner A: define exactly when ordinary versus reserved counters increment, and prove remaining safety capacity. One coherent option is to charge all receipts to ordinary quota while it is available, keep the successful reserved-safety counter at zero for those writes, and charge it only after ordinary quota is exhausted. At that point no further ordinary approvals can replenish withdrawn approvals; at most the bounded permission grants (including grants from reserved pending decisions) can be revoked once plus the currently recorded approvals withdrawn. Document the maximum and keep permission-decision lifetime counts distinct from reserve-only safety counts. Add the repeated-partial-withdrawal-before-exhaustion plus final-safety-action-at-exhaustion scenario to adapter acceptance. The 4,608-row storage bound must match the chosen counters. This is a design ambiguity with a concrete counterexample to the unsafe lifetime interpretation.

### Fresh independent evidence

```sh
PATH=/tmp/a05-runtime/bin:$PATH npm test -- tests/integration/application.test.ts packages/application/src/application.test.ts apps/web/src/command-client.test.ts tests/integration/cognito.test.ts tests/integration/http.test.ts apps/web/src/local-api-client.test.ts packages/contracts/src/contracts.test.ts
PATH=/tmp/a05-runtime/bin:$PATH npm run typecheck
```

Both commands exited 0: **120/120 focused tests, 7/7 files; typecheck passed**. Inline `node --experimental-transform-types --input-type=module` ran the 321-withdrawal probe above, exited 0, and asserted zero agreement history and fewer than 4,096 receipts. No probe modified repository files. Reviewed the exact four changed implementation/design/test files and associated tracking delta; the later `b91ff76..f2a8bf6` change is documentation only.

Implementer-reported full check: **199 unit/integration and 41 browser tests**, references 7/7, planning 15/15, boundaries 73, typecheck, build and privacy scan. It was not rerun by this reviewer because the focused checks passed and remaining findings concern the documented adapter design. No live Cognito, DynamoDB Local/AWS, IAM simulation, deployment or publication. History-slot admission is local evidence; byte quotas, counter quotas, transactional codec/races and final IAM remain unimplemented.

Documentation validation: references 7/7, planning 15/15, 105 local Markdown targets, B04/B04.5 task/board consistency and `git diff --check` passed.

### Second follow-up handoff

B04.5 remains **REVIEW / CHANGES_REQUESTED**. This review claim is finished. B04 remains PAUSED; next owner A takes bounded design corrections for R5b/R6, preserving the R1/R2a/R2b/R3/R4 and implemented history-admission closures, then obtains follow-up review before adapter expansion. Human acceptance and publication remain separate.

### Second follow-up artifact

Exact implementation/design head `b91ff767fd305a8c6bdc41957630bcf929f13e6f`; eight changed files, sorted path order, two spaces between hash/path and one newline per entry. SHA-256 `da72f47811756a1b90c2a5789ddba42a51ff355adef523c01459b86748642bb6`. Later status-only changes and reviewer evidence are outside this manifest.

```text
ed44bf8dd26ebb5df89f2413a4c70beb3bce220ff64152c12b5f087bebdfe97e  apps/web/src/command-client.test.ts
228359c604b8078df7b0c84e81d5eb56c5ed1a3d236fb216155d98d6717db40b  docs/handoff-A.md
990c623af320168a8f30c387bda35147b6def8893d38a5e07378d27e9a9e940e  docs/task-board.md
250a8f5d3915de235d7b69a59e1f7db67fc8ece3c69b45886f498e86e6f9151d  docs/tasks/B04.md
9066fcb995a4aaf297e262388a205b064d1c1aada6f02a76f703ac3ad88c3b0c  docs/work-log-A.md
50022a3530a0346d9b2a4e57212d32cd95c33c396ed04220006ce965361c0529  infra/README.md
b3f543ad6ffefe1c2e6cb0c4d733ab18a2f910f54c0d41f786dd4d10da1df824  packages/application/src/index.ts
6c16c5b1a4c55b778adce1b32ca81f36ac8eefd73f8873fada06020b3941b7ed  tests/integration/application.test.ts
```
