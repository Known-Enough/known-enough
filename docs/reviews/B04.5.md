# B04.5 — independent midpoint review

Current verdict: **PASS** for corrective source head `bfeb448`, independently reviewed 2026-09-23. R1 and R2 are closed; see the follow-up record below. The original CHANGES_REQUESTED evidence is preserved as history. This midpoint PASS does not complete B04 or establish live/cloud acceptance.

- UTC: 2026-09-23T16:38:19Z.
- Verdict: **CHANGES_REQUESTED** for the first identity slice. R1 and R2 below require B04 owner corrections and an independent follow-up review before expanding B04. This is not human acceptance or approval to publish.
- Reviewer: independent sequential reviewer in A/Ricardo's review session, separate from the B04 implementation worker. Assignment: `gpt-6-astra` / high. Actual reported model family: GPT-6 Codex; runtime variant/effort could not be independently surfaced. No concurrent implementation, application/test edits, cloud account calls, commit or publication occurred.
- Implementation owner: A/Ricardo, retained B04 claim. B04 remains PAUSED; the completed review releases only its bounded correction work to that owner. No other task is started here.
- Artifact: base `8e6d1c4103dc326acaf52a8c4ef3619642f68ae2`, source/test head `11ed27df0d9deb55700ba9520088ff403ff65508`. Review checkout: `main` at `611079a47c663153dd9b7d35a62327db871f7a77`, initially clean. The intervening commit changes tracking documents only. Review evidence changes are outside the source manifest below.

## Findings

### R1 — P1: authenticated debug mode logs identity and private owner metadata

Affected: [authenticated handler options and composition](../../apps/api/src/index.ts), especially `ApiHandlerOptions.debug` (line 41), `actor` (107–108), `logOwnerSnapshot` (119–125), and `createApiHandler` (304–312).

The new authenticated handler forwards `debug: true` into diagnostics originally intended for synthetic local identities. A successful authenticated `/rooms/:roomId/me` request writes the verified subject, room, input/draft presence, reviewed-interval count, exception-offer count, disclosure-preview count and approval state to `console.log`. Successful public reads and command paths also log the subject. This associates a real identity with owner-only state once real credentials are used, contrary to the repository's privacy/logging boundary. The option defaults to false, which limits exposure, but there is no restriction or redaction when it is enabled on the exported authenticated composition.

Independent reproduction: create a real application/in-memory room with a synthetic canary subject, serve `createApiHandler({ application, identityResolver, debug: true })`, and fetch that subject's `/me`. Status is 200; captured console output contains the subject and `exceptionOffers=` / `disclosurePreviews=`. The probe asserted this and exited 0; it did not print the captured lines or use real participant data. Existing log coverage only checks that an invalid command type is not logged.

Required correction by A/Ricardo: disable the local diagnostics for the authenticated handler or introduce an explicit safe diagnostic projection that excludes identity and private owner/consent state. Add a regression covering authenticated reads, commands and errors with diagnostic mode requested; retain permitted local synthetic diagnostics only where deliberately bounded. Follow-up must inspect the final fields, not merely assert absence of raw tokens.

### R2 — P2: committed identity tests bypass the actual security configuration

Affected: [resolver tests](../../apps/api/src/cognito-identity.test.ts) and [authenticated HTTP tests](../../tests/integration/http.test.ts); production configuration is in [the resolver](../../apps/api/src/cognito-identity.ts), lines 38–42 and 73–82.

All four committed resolver tests inject a fake `verify`, and both new HTTP tests inject a principal-returning resolver. They do not execute `CognitoJwtVerifier.create` or the environment factory. Consequently changing/removing the required client, pool or access-token constraint would leave these regressions green. The current configuration is correct by inspection and the independent offline probe below; this finding concerns missing durable coverage of the task's central security boundary, not an observed signature bypass.

Required correction by A/Ricardo: add offline signed-token/JWKS tests exercising the real configured verifier and environment factory, including wrong issuer/pool, wrong client, ID-token rejection, expired token, invalid signature and missing/invalid configuration. Use generated synthetic keys and a local key cache or injected key-fetch seam; no live Cognito is required. Keep claim mapping tests, and test that resolver failures produce a generic 401 without logging verifier/token details. Avoid replacing these assertions with a fake verifier that simply returns the expected claims.

## Reviewed behavior

- `aws-jwt-verify` is pinned to 5.2.1 in the API manifest and lockfile. The default factory supplies `userPoolId`, `clientId`, and `tokenUse: 'access'`; it does not accept issuer or role from request input. Installed dependency code verifies the signature and configured issuer/client/token use and enforces expiry/nbf when present. No AWS credentials are needed by this local verification code. Missing environment values fail startup, rather than falling back to test identities.
- Principal mapping reads only verified `sub` and group claims. The existing bounded `Id` schema accepts normal Cognito subjects. Invalid subjects, non-array/non-string group values, an empty display suffix and multiple display-prefixed groups fail closed. A single exact `deal-table:display:<room-id>` group yields only a display principal. Other groups confer no organizer/service authority. No display group yields a participant candidate whose room membership remains server-controlled. Group administration and token lifetime are separate deployment obligations below.
- Identity resolves before route/body handling (except harmless CORS preflight). HTTP route authorization calls the application before JSON parsing and replay lookup; display writes are denied before parsing. The application then rechecks operation authorization inside the transaction before strict command validation or returning a cached result. Membership removal therefore prevents replay. Organizer privileges come from the stored organizer subject, owner identity from stored subject-to-member mapping, and service operations are excluded from the HTTP principal type/mapping.
- Display credentials read only their bound room's allowlisted public projection, receive 403 for `/me` and commands, and 404 for other/absent rooms. Owner snapshots remain separately constructed. This retains the existing disclosure-audience filtering and introduces no public private-object spread or browser import of backend code.
- Resolver/token/key-fetch exceptions become null identity/401 with stable code-only responses and no verifier diagnostics. Repository/application unexpected errors retain the existing generic `INVALID_COMMAND` behavior. JWKS outages currently look like invalid credentials to clients; safe operational metrics and retry behavior remain production-composition work. No raw token logging was found. R1 concerns a different diagnostic path after successful identity resolution.
- Local synthetic identity allowlisting, loopback startup and production guard remain intact. The generic handler ignores the test-identity header; wire command/snapshot schemas are unchanged. Existing HTTP tests exercise errors, replay authorization, privacy and the complete synthetic negotiation through the shared handler.

## Fresh independent evidence

Runtime prefix used for each command: `PATH=/tmp/b04-runtime/node-v24.21.0-darwin-x64/bin:$PATH`.

| Command/check | Result |
| --- | --- |
| `node --version`; `npm --version` | v24.21.0; 11.19.0, exit 0 |
| `npm test -- apps/api/src/cognito-identity.test.ts tests/integration/http.test.ts` | Exit 0, 2 files / 19 tests passed |
| `node --experimental-transform-types --input-type=module` with an inline offline reviewer probe | Exit 0; real factory captured the exact pool/client/access constraints; generated RSA-2048 key and locally cached JWKS accepted a valid token and rejected wrong client, wrong issuer, ID token, expired token, future nbf and invalid signature; missing environment rejected |
| Same inline probe, real application + loopback HTTP handler + synthetic canary | Exit 0 reproducing R1: requested authenticated debug logs contain subject and private metadata; captured output not printed |
| `git diff --name-only 11ed27d HEAD` | Only the five tracking files from the handoff commit; no source/test delta |
| `node scripts/check-references.mjs`; local Markdown target/status/manifest audit; `git diff --check` | Exit 0; 7/7 imported reference hashes, review links/statuses and eight source hashes match; clean whitespace |

The inline probe used the real `CognitoJwtVerifier.create`, wrapping it only to prepopulate a generated JWKS cache and record its configuration. It did not replace verification with a fake success result. All signing material was synthetic and process-local. No test/application files were changed and no network key retrieval occurred. This one-off evidence does not replace R2's maintainable regression requirement.

Author-supplied historical evidence inspected in [A log](../work-log-A.md) and [B04](../tasks/B04.md): the 2026-09-23 full `PLAYWRIGHT_CHANNEL=chrome npm run check` exited 0 with references 7/7, planning 15/15, lint/import boundaries, typecheck, 187 unit/integration tests, build/privacy scan and 41 browser tests. It used the documented system Chrome fallback because bundled Chromium installation is unsupported on this macOS 12 host. That full suite was not rerun by this reviewer; focused fresh results above are independent. Review changes are documentation only.

## Remaining B04/G02 review delta

These are unimplemented/incomplete production gates, not evidence that the small identity slice already implements or fails them:

1. Define and review who may create/assign/remove display groups, configure token-generation triggers, manage subject-to-room memberships and bind single-use invitations. Untrusted users must not mint scopes or bind themselves to another owner. A group name is an application permission here; it is not an IAM policy. Cognito groups appear in signed tokens and group administration uses AWS-authorized operations. [AWS group documentation](https://docs.aws.amazon.com/cognito/latest/developerguide/cognito-user-pools-user-groups.html).
2. Specify dedicated display-account lifecycle, token storage/refresh/expiry and revocation behavior. Display reads currently trust the still-valid signed group without a server-side display-session record. Removing membership from Cognito does not rewrite an issued JWT. Revocation/sign-out is not established by signature verification; AWS explicitly documents that revoked tokens can still pass generic signature/expiry validation. Decide whether bounded token lifetime is acceptable or add application revocation checks, then test the chosen policy before G02. [AWS revocation documentation](https://docs.aws.amazon.com/cognito/latest/developerguide/token-revocation.html).
3. Review actual user-pool/client settings, issuer behavior, key rotation/outage handling, API Gateway/Lambda composition, trusted service/job identity and least-privilege IAM when those artifacts exist. The current shared Node handler still runs local queued work using a constructed service principal; it is not an SQS/production worker design. Real access tokens carry subject, client and group claims, but accepting those locally does not establish the deployed configuration. [AWS access-token documentation](https://docs.aws.amazon.com/cognito/latest/developerguide/amazon-cognito-user-pools-using-the-access-token.html).
4. Implement and review the DynamoDB repository's conditional/transaction behavior, application-enforced expiry, agreement/revocation races, duplicate commands/consumers and durable membership/invite replay. `infra` remains a placeholder and the only repository is in-memory; no IAM policy or DynamoDB implementation is approved by this review.
5. Review the corrected R1/R2 artifact independently, then the subsequent critical identity/persistence changes and actual browser/session evidence at A04.5/G02. Production CORS, abuse bounds, safe operational diagnostics and reconnect/session behavior remain unverified. G02 and human acceptance remain required before external testers.

## Reviewed source manifest

SHA-256 of all eight implementation/package/API documentation files changed by B04; each matches `11ed27d` and the review checkout. Contracts, application authorization/projection code and architecture/security references were also inspected as unchanged dependencies.

```text
f1f4bfe18ccfb855be797553215f4ffaae839628334bc0a19b1bafddf4e2225e  apps/api/README.md
3ee78ea20406d50166da50e65359d6f42d1fc14083c273fb05b7d436e84c991a  apps/api/package.json
838ba9386a95f624a18f094d611dc82318a22594071854ef385430bd55e12197  apps/api/src/cognito-identity.ts
2a8f84b80247b4a44cbd69f42710d917a6ed5131d7b9fd172d982a5b9cd617c2  apps/api/src/cognito-identity.test.ts
7a2b673412e823d16398c3e81e33057961ca1e63efba1058888c64da6575f7c1  apps/api/src/index.ts
8cacb3ad8a3d8ec4ef4a90f3d3c05ab461de4e214eb3ad0e1b9a0f91dd338d48  apps/api/src/http.ts
c3ebddd74cfd79140c7adb28f37ce78632e17d817cf810a611b9868e7c5fe5d8  package-lock.json
5106035b9cd1766151600af7f3464b3f4b1691728396fef8c834d078bbbd83dc  tests/integration/http.test.ts
```

## Independent corrective follow-up — 2026-09-23T17:02:53Z

**Verdict: PASS. R1 and R2 are addressed for this exact corrective artifact.** No new blocking finding was identified in the bounded correction. B04's midpoint review block is released; A/Ricardo retains the implementation claim and may resume the remaining B04 scope. This reviewer leaves B04 PAUSED and does not start implementation. B04.5 remains REVIEW pending human acceptance; later critical changes and G02 still require their own review.

- Reviewer: independent sequential reviewer `/root/b04_5_review`, resumed after the owner paused. Assigned target `gpt-6-astra` / high; actual exposed model family GPT-6 Codex. Runtime variant/effort are not independently exposed, so target selection is not asserted as verified. This reviewer did not implement the fixes.
- Exact correction: `611079a47c663153dd9b7d35a62327db871f7a77..bfeb4489a88363da14245d9ec19bbb23756616e2`. Review checkout was initially clean on main at `e9c08f44dda286dd14fea0b27897eec389e0634b`; the commit after `bfeb448` changes tracking documents only. Inspected the full five-file corrective diff against both original findings and the earlier source review.
- No application/test changes, personal work-log edits, cloud calls, commits, pushes or publication by this follow-up. Writes are limited to this review and coordinated B04/B04.5/board/handoff records.

### R1 closure

`ApiHandlerOptions` no longer exposes `debug`; `createApiHandler` passes literal `false` to the shared request handler. Consequently an extra runtime `debug: true` property cannot re-enable the local diagnostic paths. The local synthetic handler retains its own bounded debug behavior. New HTTP coverage deliberately supplies that extra property and performs successful public/owner reads, a successful private draft command and malformed-body error, asserting no console logging. Source inspection confirms all shared diagnostic calls use the disabled flag, including queued-work and error paths. The generic identity-failure regression also asserts no log/error/warn output. This addresses the identity/owner-metadata exposure without changing authorization or DTOs.

### R2 closure

The new tests generate two RSA-2048 key pairs, cache only the expected public JWK, sign synthetic JWTs, and invoke `createCognitoIdentityResolverFromEnv` with the real `aws-jwt-verify` verifier. They accept a valid access token and reject wrong issuer/pool/client, ID tokens, expired tokens, future nbf and a signature produced by the other key. Missing environment, malformed pool and whitespace-only client configuration fail before serving requests. Existing claim-mapping tests remain. An HTTP test uses the actual environment factory with a malformed token and checks generic 401 plus no token/error diagnostics.

The new optional `JwksCache` seam is a trusted server-composition parameter, documented as unsuitable for request data. It substitutes key retrieval/cache only; it does not bypass signature or claim validation. Inspected both cache and default factory branches: each supplies the same required pool/client and `tokenUse: 'access'` constraints. Offline signed-token tests exercise the cache branch; the no-cache branch is exercised for malformed-token rejection, not live JWKS fetching. Key rotation, cache misses/outages and deployed issuer/client behavior remain unverified production gates rather than claimed by these tests.

### Fresh follow-up evidence

Exact focused command:

```sh
PATH=/tmp/b04-runtime/node-v24.21.0-darwin-x64/bin:$PATH npm test -- apps/api/src/cognito-identity.test.ts tests/integration/http.test.ts
```

Exit 0: **2 files, 23/23 tests passed**. Runtime version checks returned Node `v24.21.0` and npm `11.19.0`, each exit 0. `git diff --name-only bfeb448 HEAD` confirmed only tracking documents after the source head. Imported reference check passed 7/7; local Markdown target/status checks, the five corrected-file hashes against `bfeb448`, preservation of personal logs/source/tests and `git diff --check` passed.

Owner-supplied full-check evidence in [B04](../tasks/B04.md): `PLAYWRIGHT_CHANNEL=chrome npm run check` passed with 191 unit/integration tests, 41 browser tests, references 7/7, planning 15/15, lint/boundaries (72 references), typecheck and build/privacy scan. This reviewer inspected that record but did not rerun or relabel the full check.

No live Cognito token issuance, user-pool configuration, JWKS retrieval/rotation, IAM, DynamoDB persistence/transaction races, invitation replay or browser session lifecycle was validated. The remaining B04/G02 review delta recorded above still applies; its R1/R2 correction requirement is now satisfied, while subsequent production/security work remains open. Human acceptance and authorization to publish are separate.

Corrected-file SHA-256 manifest (matches `bfeb448` and the follow-up checkout):

```text
a335de76db56a646baf8e1f3915305b1735313dfc79e42d28cb87d0df9d8a3ff  apps/api/README.md
44d8f8a7d9987bf12222d815a1a1082ac15bb97ae9526eefed3d7f89f49dda7f  apps/api/src/cognito-identity.test.ts
f1b151e6df0d46e39f55b379e4046d07c2bcd89a70b88d69fdf63775e5aa9812  apps/api/src/cognito-identity.ts
3b47bf17bcbb23137a4ee5e97c8395acc55a0d104ede7794a04d43c7355065f1  apps/api/src/index.ts
35e84748e4fe02220d1799d4f26f0c44b9e004e7246d7c528440fd1018501b67  tests/integration/http.test.ts
```
