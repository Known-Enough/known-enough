# A03.5 — frontend privacy and consent boundary review

Verdict: **CHANGES_REQUESTED** on current `main` at `c79faeb130ab4be5327212605197d621f572e71d`. **R1, R3 and R4 are closed for frontend preparation; R2 and R5 remain open.** The required full check also fails an outdated A03 browser assertion (V1). A02/A03 and this checkpoint remain REVIEW; A02.5/A04/A05 remain BLOCKED. Implementation corrections belong to A. Human acceptance remains pending.

## Independent follow-up — 2026-09-21T18:14:11Z

- Actual reviewer: user-assigned independent A-lane GPT-6 session; variant/effort is not exposed, so this is not attributed to a verified Astra/high run. This session did not implement the fixes and used no subagents.
- Original reviewed code: `df1e8761774b76923e9d3b69927025cbdc41c753`; correction commit: `64cdf4fc9ddadb56ad5285e6632332c6586e434c`; reviewed current main: `c79faeb130ab4be5327212605197d621f572e71d`, including B03 integration. Frontend/browser source has no delta between the correction commit and this head. The checkout was clean at review start. No fetch, merge, commit or push was performed in this review; remote equality is not asserted.
- Inspected the correction diff, current owner screen and adapters, command builders/transport, focused unit/browser tests, public proposal DTOs, contract/decision semantics, relevant product/architecture sections, import/bundle scanners and author records. Ran the full current checkpoint suite and independent browser probes. Repository writes are documentation only.
- Author reports of 25/25 browser tests remain historical evidence below/in the work log. They do **not** describe this fresh run, which failed 1/25.

| Finding | Follow-up verdict | Evidence |
| --- | --- | --- |
| R1 — receipt-only final acceptance | CLOSED for preparation | Acceptance now takes only `ProposalView`; the card displays meeting, assignments, policy, expiry and target hash from that same proposal. Production-browser inspection verified the rendered terms and submitted ID/version/hash, plus separate withdrawal. Injected no-proposal and mismatched-context reads disabled acceptance while retaining withdrawal. Existing receipt/hash mismatch unit coverage passes. |
| R2 — selected availability | OPEN, P2; partially corrected | Both unchanged-duration radio choices now submit the corresponding complete condition. The added duration path still displays a different interval than it sends; first-condition replacement also drops unrelated intervals within a hard-availability condition. Details below. |
| R3 — omitted exception policy | CLOSED for preparation | The displayed label derives from `offer.scope.policy`, as does the posted scope. The changed-policy unit test passes; an independent injected changed-policy browser probe displayed and submitted Lowest declared inconvenience without automatically sending consent. |
| R4 — unrecognized command result | CLOSED | Schema-invalid results after dispatch now throw `UnknownTransportError(command)`. Independent production probes compared raw POST bodies byte-for-byte across explicit retry after malformed JSON, schema-invalid JSON/502 and network abort. A valid structured rejection showed its safe code with no unknown-result retry. Local envelope parsing remains before dispatch. |
| R5 — explicit input confirmation | OPEN, P2; partially corrected | The initial draft requires explicit checkboxes and correctly binds draft ID/revision, owner revision and dated coverage. Editing does not produce a review of the changed draft; the old confirmation can be enabled again. Details below. |

## Remaining corrections — owner A

### R2 — P2: submitted availability still diverges from displayed or preserved inputs

[OwnerScreen](../../apps/web/src/owner-screen.tsx), lines 25–34, 93–95 and 108; [availability unit coverage](../../apps/web/src/owner-mock-adapter.test.ts), line 40.

1. On `/?view=owner`, choose 60 minutes, select the radio still labelled **11:00–11:30 is available**, and submit. The intercepted condition covers **11:00–12:00** (`endMinute: 720`). `selectedInterval` uses the edited duration, but both radio labels render the original `interval`. The original ignored-radio defect is fixed, but the full interval displayed to the owner still does not match the payload after an ordinary supported edit.
2. Inject a valid owner draft whose first HARD_AVAILABILITY condition has Thursday 11:00–11:30 **and** Sunday 10:00–11:00 intervals, followed by a separate negotiable condition. Submit without editing. `valuesForAvailability` replaces the entire first condition with `[selectedInterval]`, silently deleting Sunday availability. Switching that first condition to the exception choice replaces its hard envelope altogether. The new unit test has only one original condition/interval, so its claim to preserve unrelated inputs does not exercise this case.

Required: render the exact interval that will be submitted, target the intended condition/interval explicitly, and preserve unrelated intervals/conditions and hard exclusions. Cover both radio choices, the 60-minute display/payload agreement, and a valid multi-condition/multi-interval input. This is a frontend mapping defect; no assertion that the server accepts a hard-condition bypass is made.

### R5 — P2: editing allows confirmation of the old draft instead of review of the changed values

[OwnerScreen](../../apps/web/src/owner-screen.tsx), lines 81–95 and 108–109; [duration regression test](../../tests/e2e/a01.spec.ts), line 91.

Reproduction on `/?view=owner&owner=draft`, with command responses intercepted:

1. Check the 11:00–11:30 draft interval, then change duration to 60 minutes. The confirmation button initially disables, as the new test asserts.
2. Submit the changed input draft; its condition ends at 12:00. The confirmation region still shows `draft-nina-2`, revision 2, and only 11:00–11:30.
3. Check that old interval again. Confirmation becomes enabled and sends `CONFIRM_INPUTS` for **the old draft/revision and `endMinute: 690`**, despite the 60-minute form. This is also possible before submitting the edit. No 11:00–12:00 review is offered.

A related production probe checked the draft, then changed the displayed duty cost from 1 to 3. Confirmation stayed enabled and still targeted the saved revision-2 draft containing cost 1; the confirmation region does not show that saved cost or distinguish it from the edited form.

`draftIntervals` and the command always use `room.draft`, while duration, availability and cost live in separate editable state. Clearing a checkbox alone does not bind a new review to changed values. The current duration test stops at the disabled button and misses the subsequent stale recheck. This does not demonstrate server acceptance of stale commands; a real server may reject them. The missing current-draft review is reproducible entirely in preparation against synthetic/injected reads.

Required: distinguish saved draft terms from unsaved edits and prevent confirmation of a misleading/stale target. After submission, obtain or inject the resulting draft/owner revision and display its full conditions/preferences and dated intervals; require explicit review of the new interval before confirming. A changed 60-minute draft must either remain safely unconfirmable pending a current read or offer a fresh 11:00–12:00 review bound to its new draft revision. Extend tests through edit → submit/current snapshot → review → confirm, including cost/availability edits. Do not infer reviewed coverage from the schedule.

### V1 — P2: the required checkpoint suite fails the A03 receipt assertion

[Receipt browser test](../../tests/e2e/a03.spec.ts), line 9.

Both the full check and a focused rerun fail waiting for `Synthetic local receipt:`. The R1 correction removed that text; current code renders proposal terms plus the separate private receipt card. The failure occurs before this test reaches its public-isolation assertions. This is an outdated test expectation, not evidence that the receipt disappeared or leaked.

Update the test to assert the intended separate receipt history and proposal terms, retain the subsequent public-isolation checks, and rerun the focused tests plus the full checkpoint suite. Do not restore receipt-only acceptance to satisfy the old assertion.

## Fresh follow-up verification

Pinned Node **24.21.0**, npm **11.19.0** via `PATH=/Users/martelaxe/.nvm/versions/node/v24.21.0/bin:$PATH`. Installed Chrome **150.0.7871.125** was used under the documented macOS fallback.

| Actual command/check | Result |
| --- | --- |
| `npx playwright install chromium` | Exit 1: Chromium unsupported on mac12; used documented installed-Chrome fallback |
| `PLAYWRIGHT_CHANNEL=chrome npm run check` | **Exit 1**: reference hashes 7/7, planning 15/15, lint/import boundaries 55 references, TypeScript, unit/integration **169/169 in 10 files**, build 124 modules and bundle scan all passed; browser **24/25**, V1 failed |
| `PLAYWRIGHT_CHANNEL=chrome npx playwright test tests/e2e/a03.spec.ts` | **Exit 1**: same missing-text failure, 1/2 passed; confirms the failure independently of the full run |
| `node /tmp/known-enough-A03.5-followup-probes.mjs` | Exit 0: **20 asserted observations**, including remaining R2/R5 defects, closed R1/R3/R4 cases, and public isolation across six scenarios; four synthetic owner markers also confined to the owner production chunk |
| Final documentation validation; `npm run check:references`; `git diff --check` | Exit 0: 92 local file-link targets across 10 changed documents resolve; task gates consistent; original review preserved verbatim; reviewed HEAD unchanged; reference hashes 7/7; no whitespace errors or executable changes |

The independent script uses production preview on port 4174 for ordinary flows, raw retry comparisons and public checks. Four cases use an isolated Vite dev instance on port 4175 with in-memory adapter responses (absent proposal, mismatched owner context, changed policy, multi-interval owner draft); no application/test source was altered. Exit 0 confirms the stated observations, including defects, **not** a passing acceptance verdict. Reproduction instructions above remain usable without the temporary script. Machine-local diagnostics: `/tmp/known-enough-A03.5-followup-check.log`, `/tmp/known-enough-A03.5-followup-a03.log`, `/tmp/known-enough-A03.5-followup-probes.mjs`, `/tmp/known-enough-A03.5-followup-probes.log`.

Public parsing and import boundaries still hold. Across collecting, blocked, private-review, proposed, agreed and superseded production views, no owner screen chunk was requested and no probed owner markers/receipt text appeared in DOM/console. Mobile and reduced-motion checks passed. Synthetic owner assets remain downloadable; lazy loading is not authentication. Fixed mock snapshots, synthetic decision revision, actual API projection refresh, authenticated isolation, server transaction/expiry enforcement and live negotiation remain separate gates; integrated B03 does not establish browser/API acceptance by itself.

Route R2/R5 and V1 to A, then independently review the changed artifact and rerun affected checks plus the full checkpoint suite. A02/A03/A03.5 stay REVIEW; A02.5/A04/A05 stay BLOCKED. B03/A06 retain their separate gates. Review claim is finished, with no implementation claim taken. No publication or human acceptance is implied.

## Historical initial review — original `df1e876` artifact

The following original findings and test results are preserved as dated evidence. Their original five-open-finding status is superseded by the follow-up above.

Verdict: **CHANGES_REQUESTED**. Five open findings below prevent release of A04/A05 preparation and A02.5. Existing checks pass, but additional browser probes expose defects those checks do not cover. Fixes belong to implementation owner A; this reviewer changed documentation only. Human acceptance remains pending.

## Reviewed artifact and independence

- Review completed 2026-09-21T16:58:49Z by the user-assigned independent A-lane reviewer, GPT-6 Astra / high. This session did not implement A02/A03 and used no subagents.
- Base: `f36df74f975f5cd44a1f0d2017d1aa89cb16c561`.
- A02: `6499ec46b3fbcb3ac10b08316fb354e9f6601639`.
- Head/A03: `df1e8761774b76923e9d3b69927025cbdc41c753`. Clean local main matched origin/main after `git fetch origin`; no integration or publication performed. Subsequent review documentation does not change this code artifact.
- Reviewed the complete frontend delta plus App/public/owner adapters and screens, client builders/transport, unit/browser tests, production assets, import/bundle scanners, contracts and relevant product/architecture consent sections. Read A02/A03 tickets, A handoff/log, board and integration record. The current workflow's preparation/live split supersedes the older architecture schedule.
- Author evidence is retained in [work log A](../work-log-A.md): A02 reported 152 unit/integration and 18 browser tests; A03 reported 152 and 20, with npm 8.19.2. The fresh reviewer run below used both pinned versions and does not relabel those author runs.

## Open findings — owner A

### R1 — P1: final acceptance is enabled without the proposal terms

[OwnerScreen](../../apps/web/src/owner-screen.tsx), line 73; [acceptProposal](../../apps/web/src/command-client.ts), line 70.

In `/?view=owner&owner=approval`, “Confirm this exact approval” sends ACCEPT_PROPOSAL using `room.ownApproval`. The card shows an ID/version/hash from an existing receipt, but no full meeting, duty assignments or policy. There is no current ProposalView loaded or checked. The helper expressly accepts a FinalApproval as an alternative to proposal facts. A02 introduced this actionable path where A01 only displayed history. In the ordinary owner scenario, a first approval is unavailable entirely.

Browser reproduction: intercept the command route with a schema-valid success, open the approval scenario, inspect the final-acceptance card, then click the confirmation button. The probe confirmed an ACCEPT_PROPOSAL was sent despite the missing terms. Withdrawal emits its own WITHDRAW_APPROVAL command correctly.

Required correction: retain the receipt as history and bind new acceptance to an explicitly reviewed current public proposal with full terms and matching context/version/hash. Until that preparation is available, keep acceptance disabled; withdrawal may continue to use the existing receipt. Add coverage that receipt-only data cannot enable acceptance and that displayed terms match the submitted target. A hash by itself does not establish informed consent.

### R2 — P2: availability selection is ignored when building a draft

[OwnerScreen](../../apps/web/src/owner-screen.tsx), lines 55 and 70; [existing form test](../../tests/e2e/a01.spec.ts), line 44.

The radio changes `availability`, but `values()` only copies the existing conditions and changes the duty cost. Selecting “is available” therefore still submits the original NEGOTIABLE_UNAVAILABLE condition. The UI reports acceptance of a draft opposite to the displayed choice. The existing browser test selects this radio but asserts only the cost and command types, so it passes.

Browser reproduction: open `/?view=owner`, select “is available”, submit the draft and inspect the intercepted payload. The probe confirmed the radio stayed selected while the submitted condition stayed unavailable.

Required correction: map the selected full interval into the intended finite condition representation, preserving unrelated conditions and hard exclusions. Add assertions for both radio choices and the complete relevant conditions, not just the cost.

### R3 — P2: exception permission binds a policy the preview no longer displays

[OwnerScreen](../../apps/web/src/owner-screen.tsx), line 71.

A02 removed the policy label from the exception scope preview. The posted offer still binds `offer.scope.policy`, but the private screen displays only the roster, revision, interval, no-duty predicate and expiry. The agreed ranking policy is a material part of the exception scope and a policy change invalidates consent. The previous screen displayed it.

Browser reproduction: open `/?view=owner`, inspect the Exception region and click “Allow scoped exception” with intercepted HTTP. The command includes the balance-load policy; the region contains neither its label nor any policy text.

Required correction: restore the human-readable policy from the same scope used for the command. Assert that the preview and posted scope agree, including a changed-policy example, without replaying previous consent.

### R4 — P2: an unrecognized JSON response loses the original retry envelope

[browserCommandTransport](../../apps/web/src/command-client.ts), line 22; [sendCommand](../../apps/web/src/command-client.ts), line 84; [OwnerScreen.run](../../apps/web/src/owner-screen.tsx), line 41.

Fetch failures and JSON decoding failures become UnknownTransportError, but successful JSON decoding followed by CommandResult validation failure does not. A gateway can return JSON outside the result contract after a request was forwarded; the client cannot conclude whether the mutation applied. Instead it displays “could not be prepared or sent,” offers no unchanged retry, and generates a new idempotency key on the next click.

Browser reproduction: intercept the command response as HTTP 502 with `{"upstream":"unknown"}`. After one exception submission there is no “Retry unchanged request” control. Submitting again creates a different key. In contrast, a separate network-abort probe confirmed that the implemented explicit retry preserves the complete original serialized envelope byte-for-byte.

Required correction: preserve the original envelope whenever no trustworthy result can be established after dispatch, including invalid result shapes; offer the same explicit unchanged retry. Distinguish local pre-dispatch validation failures and valid server rejections. Add transport/browser tests for malformed JSON, valid JSON outside the schema and normal structured errors.

### R5 — P2: explicit input-confirmation preparation is absent

[OwnerScreen](../../apps/web/src/owner-screen.tsx), line 70; [command builders](../../apps/web/src/command-client.ts), line 58.

The frontend stops at SUBMIT_INPUT_DRAFT. Neither its source nor browser tests contain CONFIRM_INPUTS or reviewedIntervals, and there is no review/confirmation control in the draft scenario. This leaves the explicit owner-reviewed coverage requirement from [main integration](../main-integration.md#integration-changes-and-review) and [contracts](../contracts.md#logical-http-surface-and-command-preconditions) unimplemented. This is a missing preparation step, independent of whether B03 is running; no real server is needed to exercise it through injected responses.

Required correction: provide a separate draft review/confirmation action binding draft ID/revision and owner revision, with explicitly assessed full dated intervals. Never derive consent by automatically copying every schedule option. Cover confirmation against intercepted responses and require fresh review after a changed duration. Actual local negotiation remains A02.5 work.

## Fresh verification

Runtime: Node **24.21.0**, npm **11.19.0**. Commands used `PATH=/Users/martelaxe/.nvm/versions/node/v24.21.0/bin:$PATH`.

| Command/check actually run | Result |
| --- | --- |
| `git fetch origin`; compare HEAD/origin/main | Exit 0; both at reviewed head |
| `npx playwright install chromium` | Exit 1: Playwright does not support chromium on mac12 |
| Installed Chrome `--version` | 150.0.7871.125; documented macOS fallback |
| `PLAYWRIGHT_CHANNEL=chrome npm run check` | Exit 0: reference hashes 7/7; planning 15/15; lint/import boundaries 46 references; TypeScript; unit/integration 152/152 in 9 files; build 124 modules and bundle scan; browser 20/20 |
| `node /tmp/known-enough-A03.5-probes.mjs` against a separate production preview on port 4174 | Exit 0: 12 targeted probes; four reproduced defects R1–R4, separate withdrawal and exact network retry passed, six public scenarios passed privacy observations |
| Additional Node assertions over built public/owner JS chunks | Exit 0: four synthetic owner value markers absent from public entry and present in owner chunk |
| `rg -n 'CONFIRM_INPUTS\|reviewedIntervals\|ACCEPT_CONTEXT\|REQUEST_SOLVE' apps/web/src tests/e2e` | No matches; R5 confirmed by source and UI inspection |

The probe script asserts the observed defects, so its exit 0 is evidence of reproduction, not a passing acceptance verdict. Machine-local diagnostics: `/tmp/known-enough-A03.5-check.log`, `/tmp/known-enough-A03.5-probes.mjs`, `/tmp/known-enough-A03.5-probes.log`. Reproduction steps above remain usable without those temporary files. No application/test source was changed to obtain these results. Final documentation links, status consistency, reference hashes and whitespace were checked separately before handoff.

## Boundaries that held and remaining limits

Public snapshots are independently parsed through strict allowlisted schemas, never constructed from private object spreads. The frontend imports contracts and UI dependencies, with no server/test-support fixture imports. Across collecting, blocked, private-review, proposed, agreed and superseded browser scenarios, no owner chunk was requested and no owner value markers or receipt text appeared in the DOM/console. Production asset inspection confirmed owner mock values remain in the lazy owner chunk. That downloadable chunk contains deliberately synthetic examples; lazy loading is not access control. The existing bundle scan detects seeded server markers only and is not a general privacy proof.

Exception and disclosure commands are separate and carry no client owner-identity field. The disclosure preview preserves wording, audience and inference warning. Rendered errors use safe codes/generic text; source inspection found no frontend console logging, analytics or local/session storage of owner payloads. The public UI has no mutation controls. Existing stale-response tests confirm no automatic replay, and stale read controls remain disabled until refresh. Mobile overflow, keyboard navigation and reduced-motion checks pass.

Missing evidence: current snapshots still come from fixed owner mocks and a hardcoded synthetic decision revision; a stale refresh does not demonstrate receipt of a newer real context. Successful commands do not establish updated server projections. A03 exercises active and empty receipts, but provides no transition coverage for declined/revoked/expired/superseded history or actual publication. No authenticated isolation, real API negotiation, transaction/expiry enforcement or live acceptance is established. Those later gates remain distinct from these frontend findings.

A02/A03 remain REVIEW; A03.5 is REVIEW with CHANGES_REQUESTED. A02.5, A04 and A05 remain BLOCKED. Route R1–R5 to A for a bounded implementation follow-up, then independently review the new named artifact and rerun affected checks plus the checkpoint suite before recording PASS. B03 and A06 are not blocked by this verdict. No push, PR, merge, deployment or human acceptance is implied.
