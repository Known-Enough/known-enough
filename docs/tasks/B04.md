# B04 — Cognito adapter, room membership/display scopes, DynamoDB repository, race/idempotency tests

- Execution: Select **GPT-6 Astra** as task lead. Read [Astra-led task execution](../agent-workflow.md). Astra is explicitly authorized to choose and spawn available local subagents and their model/reasoning effort for bounded subtasks, or work directly when appropriate. Astra owns integration, verification and the final handoff; existing task scope, dependencies and human review still apply.

- Owner: B
- Status: BLOCKED — dependencies not implemented
- Branch: `task/b04` (feature branch is suggested, not created)
- Dependencies: G01
- Objective: Cognito adapter, room membership/display scopes, DynamoDB repository, race/idempotency tests
- Allowed files: packages/adapters/**, apps/api/**, tests/integration/**, infra/** (no deployment without approval)
- Acceptance checks: Run relevant unit/integration/browser tests and npm run check; record actual outputs. Test verified identity, cross-room/owner isolation, invite replay, display scope, auth-before-cache, real-adapter transaction races and application-enforced expiry. No paid resource creation without authorization.
- Non-goals: no unrelated lane changes, no silent contract/root changes, no publishing/deployment/spending without explicit authorization. Implement only this ticket; preserve independent consent and private/public boundaries.
- Handoff: Next dependent task from the architecture board, after review.. Provide named diff/commit when available, exact test outcomes, contract changes and remaining limitations. Root/lockfile/CI belong to A; contracts belong to B after bootstrap, with A reviewing breaking changes.
