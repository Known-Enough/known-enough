# B04 — Cognito/DynamoDB adapters, scopes, and race/idempotency tests

- Direct worker: human selects `gpt-6-sol`, high effort; record actual model in evidence.
- Pool: Shared; either user may claim eligible unclaimed work. Historical claims below remain evidence; active claims require explicit release.
- Status: IN_PROGRESS — resumed after independent B04.5 PASS; bounded next slice is the conditional DynamoDB room repository and replay-data minimization. Later identity/session lifecycle, invitations, IAM, expiry and race acceptance remain open.
- Claim: A / Ricardo resumed 2026-09-23 from baseline `590ae57` on `main`. Actual session model GPT-6 Codex; variant/effort not exposed. Ticket target remains `gpt-6-sol` / high; runtime target selection is not claimed verified. This slice permits `packages/adapters/**`, `packages/application/src/index.ts`, `packages/application/src/application.test.ts`, root `package-lock.json`, adapter/application tests, `infra/README.md`, and this worker's log plus coordinated B04/board/handoff records. Root lockfile is coordinated within B04. No cloud calls/resources, IAM, deployment, wire-contract or browser changes in this slice.
- Prerequisite: G01. B04.5 is mandatory after design and the first reviewable implementation slice, before expanding the identity, transaction, or IAM approach.
- Scope/files: `packages/adapters/**`, `apps/api/**`, `packages/application/**` only when needed for internal replay privacy/transaction correctness, `tests/integration/**`, `infra/**`, package lockfile as coordinated, and the implementing developer’s work log plus task/board/handoff records. No deployment without explicit approval; coordinate contracts/root changes.
- Acceptance: verified identity, cross-room/owner isolation, invite replay, display scope, auth-before-cache, real-adapter transaction races, and application-enforced expiry. Address B04.5 findings; run relevant unit/integration tests and `npm run check`, recording actual outcomes. Do not fake cloud evidence.
- Handoff: B04.5, then B05/A04.5/G02.

Follow [agent workflow](../agent-workflow.md) and [task board](../task-board.md). Future work uses a separate clone on `main`; no mandatory branch or subagent.

## Current gate reconciliation — 2026-09-23

G01 is DONE for its accepted local UI/API/domain checkpoint. Independent follow-up PASS and human acceptance are recorded in [G01](G01.md) and [its review](../reviews/G01.md). B04's G01 prerequisite is met. B04's first identity slice and R1/R2 corrections received independent B04.5 follow-up PASS; the midpoint review block is released. Production authentication composition, durable persistence, real DynamoDB races, and deployment remain open gates.

### First-slice design boundary

The initial slice verifies Cognito access-token signatures and the configured pool/client before deriving a principal from the signed subject and group claims. A single `deal-table:display:<room-id>` group grants only that room's existing read-only display principal; ambiguous or malformed display scopes fail closed. All other verified subjects are participant candidates, with actual room membership and organizer permissions still enforced by the application. The generic API handler resolves identity before route/body handling, then performs application route authorization before parsing command bodies or reaching idempotency replay. No user-supplied owner ID is accepted. B04.5 must review this claim format, user-pool assumptions, handler ordering, tests, and operational failure behavior before further identity/IAM or persistence work.

### First-slice result — 2026-09-23

Implemented `createCognitoIdentityResolver`, environment configuration, a generic authenticated API handler, strict display-group mapping, and focused resolver/HTTP scope regressions. Added pinned dependency `aws-jwt-verify@5.2.1`. Files: `apps/api/package.json`, `apps/api/src/cognito-identity.ts`, `apps/api/src/cognito-identity.test.ts`, `apps/api/src/http.ts`, `apps/api/src/index.ts`, `apps/api/README.md`, `package-lock.json`, and `tests/integration/http.test.ts`. Local review head: `11ed27d` (base `8e6d1c4`); not published.

Checks: pinned Node `24.21.0` / npm `11.19.0`; focused `npm test -- apps/api/src/cognito-identity.test.ts tests/integration/http.test.ts` passed 19/19. Full `PLAYWRIGHT_CHANNEL=chrome npm run check` passed: imported references 7/7, planning 15/15, lint/import boundaries, typecheck, 187 unit/integration tests, production build/privacy scan, and 41 browser tests using installed system Chrome. Playwright Chromium installation is unsupported on this macOS 12 host; the documented Chrome fallback was used. No AWS account or cloud API was contacted, and no cloud resource was created. Independent critical review has not yet occurred; B04.5 is the next gate.

## Authorized integration — September 22, 2026

This dated integration note predates G01 acceptance and B04's current claim; its unclaimed/pending-acceptance wording below is historical.

User authorized commit/push of the reviewed repairs in `d042d8f`; synchronization found published overlapping work through `8371e7b`. Both histories are preserved in the integration. Latest implementation owner: B for this bounded integration; no new task takeover is inferred from the shared-pool policy. A02.5/G01 repair evidence is in [G01](../reviews/G01.md). Incoming diagnostics/readiness fixes and coverage-review regression are preserved; initial answers stay explicit per interval, retries remain exact and snapshots must match control versions. Fresh combined `npm run check` passed 179 unit/integration and 37 browser tests; independent follow-up PASS on the exact 12-file artifact is recorded in G01. Publication is explicitly user-authorized; human checkpoint acceptance remains separate. B04 remains unclaimed pending G01 acceptance. Earlier findings remain historical evidence.

## B04.5 correction handoff — 2026-09-23

Independent [B04.5 review](../reviews/B04.5.md) returned CHANGES_REQUESTED on `11ed27d`. Correct authenticated debug privacy (R1/P1) and add actual verifier/configuration regressions (R2/P2), then obtain independent follow-up review. A/Ricardo retains the claim; this review did not resume implementation. Live identity/session lifecycle, invites, IAM and durable transaction evidence remain open B04/G02 work.

## B04.5 corrections claimed — 2026-09-23

Owner resumed only the R1/R2 correction scope from baseline `611079a`. Remove authenticated diagnostic logging even if an untyped runtime caller supplies `debug: true`; retain local synthetic diagnostics. Add generated-key offline signed-token regressions through the actual Cognito environment factory and ensure verifier failures map to generic 401 with no diagnostic logging. Independent follow-up is the next gate after focused and full checks. No cloud calls or production claims.

## B04.5 correction handoff — 2026-09-23

Corrective artifact committed locally as `bfeb448` (`611079a..bfeb448`); B04 is PAUSED pending independent follow-up. Authenticated composition no longer exposes or forwards debug mode; local synthetic diagnostics remain. Offline RSA-signed JWT tests use a preloaded synthetic JWKS through the real environment factory and cover valid access, wrong issuer/pool/client, ID token, expiry, nbf, signature, missing/invalid configuration, and generic 401 without log details. The focused API/HTTP suite passed 23/23. Full `PLAYWRIGHT_CHANNEL=chrome npm run check` passed with references 7/7, planning 15/15, lint/boundaries (72 references), typecheck, 191 unit/integration tests, build/privacy scan and 41 browser tests. Pinned Node 24.21.0/npm 11.19.0; no AWS calls, cloud resources, push or publication. Next: B04.5 follow-up review on this exact source diff before resuming B04.

## Independent follow-up PASS — 2026-09-23

The independent [B04.5 follow-up](../reviews/B04.5.md) reviewed `611079a..bfeb448`, closed R1/R2 and reran the pinned focused suite: 23/23 passed. B04 remains PAUSED with A/Ricardo's claim retained for resumption; the midpoint review block is released. Subsequent critical changes still need review, and live identity/session/invite, IAM and DynamoDB/G02 evidence remain outstanding. No B04 expansion or human acceptance is recorded by this review.

## B04 resumption — 2026-09-23

User directed continuing after the midpoint finding was fixed. B04.5 follow-up PASS is recorded on `611079a..bfeb448`; review claim is released. B04 resumes at baseline `590ae57` with one bounded slice: conditional per-room DynamoDB persistence plus a non-plaintext replay comparison digest and focused adapter/application tests. Official AWS constraints reviewed: DynamoDB conditional writes support optimistic version checks; item size is limited to 400 KB. Prefer one atomic room aggregate item for this slice, with an internal storage version fence and bounded retries on conflicts; document the aggregate-size/contended-room limitation. Do not claim production races without running the real adapter against DynamoDB Local and/or a real service. No deployment, AWS account calls, IAM, wire/API or cloud resource creation.
