# B03 — local HTTP server, test identities explicitly non-production, request validation and error contracts

- Execution: Select **GPT-6 Astra** as task lead. Read [Astra-led task execution](../agent-workflow.md). Astra is explicitly authorized to choose and spawn available local subagents and their model/reasoning effort for bounded subtasks, or work directly when appropriate. Astra owns integration, verification and the final handoff; existing task scope, dependencies and human review still apply.

- Owner: B
- Status: BLOCKED — dependencies not implemented
- Branch: `task/b03` (feature branch is suggested, not created)
- Dependencies: B02
- Objective: local HTTP server, test identities explicitly non-production, request validation and error contracts
- Allowed files: apps/api/**, tests/integration/**; contracts with A review
- Acceptance checks: Run relevant unit/integration/browser tests and npm run check; record actual outputs. Verify all documented HTTP code mappings, schema rejection, missing identity, non-enumerating missing/other-owner resources, display writes and organizer escalation. Local test identities must be labeled non-production.
- Non-goals: no unrelated lane changes, no silent contract/root changes, no publishing/deployment/spending without explicit authorization. Implement only this ticket; preserve independent consent and private/public boundaries.
- Handoff: G01 with A02. Provide named diff/commit when available, exact test outcomes, contract changes and remaining limitations. Root/lockfile/CI belong to A; contracts belong to B after bootstrap, with A reviewing breaking changes.
