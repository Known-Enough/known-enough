# B02 — application commands, in-memory repository, owner/public projections, versioned consent tests

- Execution: Select **GPT-6 Astra** as task lead. Read [Astra-led task execution](../agent-workflow.md). Astra is explicitly authorized to choose and spawn available local subagents and their model/reasoning effort for bounded subtasks, or work directly when appropriate. Astra owns integration, verification and the final handoff; existing task scope, dependencies and human review still apply.

- Owner: B
- Status: BLOCKED — dependencies not implemented
- Branch: `task/b02` (feature branch is suggested, not created)
- Dependencies: B01
- Objective: application commands, in-memory repository, owner/public projections, versioned consent tests
- Allowed files: packages/application/**, packages/adapters/** (in-memory only), tests/integration/**; contracts with A review
- Acceptance checks: Run relevant unit/integration/browser tests and npm run check; record actual outputs. Test owner/public projections, stale contexts/proposals, same-key/different-body replay, independent permissions, expiry/decline/withdrawal/closed-room behavior, semantic invalidation and competing finalization/revocation using an injectable clock. In-memory tests do not prove DynamoDB safety.
- Non-goals: no unrelated lane changes, no silent contract/root changes, no publishing/deployment/spending without explicit authorization. Implement only this ticket; preserve independent consent and private/public boundaries.
- Handoff: B03 and A02. Provide named diff/commit when available, exact test outcomes, contract changes and remaining limitations. Root/lockfile/CI belong to A; contracts belong to B after bootstrap, with A reviewing breaking changes.
