# A01 — shared-table and owner screens with mock API adapter

- Execution: Select **GPT-6 Astra** as task lead. Read [Astra-led task execution](../agent-workflow.md). Astra is explicitly authorized to choose and spawn available local subagents and their model/reasoning effort for bounded subtasks, or work directly when appropriate. Astra owns integration, verification and the final handoff; existing task scope, dependencies and human review still apply.

- Owner: A
- Status: BLOCKED — awaiting accepted F02 baseline
- Branch: `task/a01` (feature branch is suggested, not created)
- Dependencies: F02
- Objective: shared-table and owner screens with mock API adapter; loading/error/stale states; keyboard usable
- Allowed files: apps/web/**, tests/e2e/**; public UI mocks only
- Acceptance checks: Run relevant unit/integration/browser tests and npm run check; record actual outputs. Show shared-table and owner surfaces through separate public/owner adapters; loading, empty, failure and stale states; usable keyboard/mobile navigation; display exact timezone and consent boundaries. Owner mock values must never be imported by the shared screen. Do not enable impersonation or label local mocks authenticated.
- Non-goals: no unrelated lane changes, no silent contract/root changes, no publishing/deployment/spending without explicit authorization. Implement only this ticket; preserve independent consent and private/public boundaries.
- Handoff: A02, waiting on B02/B03 for real HTTP. Provide named diff/commit when available, exact test outcomes, contract changes and remaining limitations. Root/lockfile/CI belong to A; contracts belong to B after bootstrap, with A reviewing breaking changes.
