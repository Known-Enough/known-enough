# B04.5 — midpoint identity, transaction, and IAM review

- Direct worker: human selects `gpt-6-astra`, high effort; record actual model in evidence.
- Pool: Shared; either user may claim eligible unclaimed work. Historical claims below remain evidence; active claims require explicit release.
- Status: REVIEW — independent PASS for bounded persistence slice `a3e7261..cb2dbf9`; human acceptance and remaining B04/G02 requirements are open.
- Claim: Independent sequential reviewer `/root/b04_5_review` completed and released this checkpoint 2026-09-23. Assigned `gpt-6-astra` / high; actual exposed family GPT-6 Codex, runtime variant/effort not independently exposed. B04 remains PAUSED with A/Ricardo retaining implementation ownership; no other task started.
- Initial review artifact: base `8e6d1c4`, head `11ed27d` on local `main`; see the original verdict below. Prior identity correction artifact: `611079a..bfeb448`; current persistence artifact: `a3e7261..cb2dbf9`; local `main` is unpublished to `origin/main`.
- Prerequisites: G01 and a paused, reviewable B04 implementation slice.
- Scope/files: review source diff `a3e7261..cb2dbf9` for the persistence slice alongside the recorded identity baseline and previous R1/R2 PASS. Inspect the adapter, replay behavior, privacy, tests, aggregate/item constraints, and current IAM/operational gaps. Write `docs/reviews/B04.5.md` and coordinated B04.5/board/handoff status only. Do not change source/tests or write to A/B personal work logs; route any fixes to B04's owner.
- Acceptance: independent critical review of identity, transactions, IAM, design, code, and tests. Run focused adapter/application/API tests and, if the supplied local runtime is available, independently exercise the opt-in DynamoDB Local path. Record actual commands/results and distinguish emulator evidence from unrun managed-service/cloud checks. Report findings and the remaining delta required for G02; B04 completion requires critical findings addressed and actual checks.
- Handoff: G02.

Follow [agent workflow](../agent-workflow.md) and [task board](../task-board.md). Use a separate clone on `main`; no mandatory subagents.

## Independent verdict — 2026-09-23

[Review record](../reviews/B04.5.md): **CHANGES_REQUESTED** on `8e6d1c4..11ed27d`. R1 (P1): authenticated debug mode logs verified subject and private owner metadata. R2 (P2): committed tests bypass the real Cognito verifier/configuration; add durable offline security regressions. A/Ricardo owns corrections, followed by independent review of the changed artifact before B04 expands. The review does not resume B04 or claim another task.

Fresh pinned API/HTTP suite: 19/19 passed. Independent offline real-verifier probe passed signature/issuer/client/token-use/expiry/nbf checks; synthetic loopback probe reproduced R1. No live Cognito, DynamoDB, IAM, deployment or cloud race evidence. Source manifest, supplied full-check results and remaining G02 delta are in the review. Human acceptance remains pending.

## Corrective artifact under independent follow-up — 2026-09-23

B04 is PAUSED after owner commit `bfeb448`; the next action is independent follow-up against `611079a..bfeb448`. Preserve the original CHANGES_REQUESTED verdict and add a dated follow-up verdict/evidence when review finishes. No B04 expansion begins until follow-up PASS.

## Independent follow-up verdict — 2026-09-23

[Follow-up review](../reviews/B04.5.md): **PASS** on `611079a..bfeb448`; R1 and R2 closed. Authenticated runtime debug cannot enable local diagnostics. Signed synthetic-token tests exercise the actual verifier/environment factory and reject invalid signature, issuer/pool, client, token use and time claims; configuration/error privacy regressions pass. Fresh pinned API/HTTP tests: 23/23, exit 0. No source/test or personal-log edits by the reviewer.

B04's midpoint review block is released for A/Ricardo's remaining scope; the reviewer did not resume implementation. Live Cognito, session/invite lifecycle, IAM, DynamoDB and G02 acceptance remain open. The earlier CHANGES_REQUESTED record is historical; human acceptance/publication remain separate.

## First conditional persistence slice — 2026-09-23

B04 paused after source commit `cb2dbf9` (`a3e7261..cb2dbf9`). The owner recorded focused tests, full-check evidence and an actual DynamoDB Local 3.3.1 run in [B04](B04.md). This sequential review is active; inspect the named source diff and tests. No managed DynamoDB, IAM, production authentication/session lifecycle, or cloud acceptance is claimed. The previous identity-slice PASS remains evidence only for its exact reviewed scope.

## Independent persistence verdict — 2026-09-23

[Persistence review](../reviews/B04.5.md): **PASS for the bounded aggregate adapter slice `a3e7261..cb2dbf9`**. Fresh focused suite: 41 passed, one opt-in skipped; independent DynamoDB Local 3.3.1 SDK test: 1/1 passed. Additional emulator application probe verified 12 identical concurrent commands commit once, changed-body conflict, and authorization before cached replay after membership removal. Tables deleted and Local stopped; no cloud evidence claimed.

The 400 KiB limit was independently reproduced; enforced capacity/retention and safe withdrawal/revocation behavior, persisted semantic races, schema migration, session/invite lifecycle and IAM remain required before production/G02 acceptance. Prior R1/R2 closures remain valid. B04 remains PAUSED for owner resumption; this reviewer releases only the checkpoint block, not human acceptance or publication.
