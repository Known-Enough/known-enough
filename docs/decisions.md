# Bootstrap decisions and risks

- Intended application workspace: the user's open, empty `/home/martelaxe/known-enough`. Inspection found no existing files, applicable parent AGENTS or enclosing Git repository. Initialized a local main branch only. No existing work overwritten, no remote configured, no publish or commit performed.
- All seven requested references were accessible under the WSL Windows mount. The user-supplied task path omitted the separators before `.codex` and `.chatgpt-projects`; the task's own source list resolved correctly. Copied, never moved. Source/destination byte equality was checked at import; SHA-256 manifest supports portable integrity checks. Original absolute paths remain in immutable source documents as provenance, never in scripts/config/runtime imports. No account configuration or source mirror AGENTS imported.
- Product and architecture were read completely; no material consent/privacy contradiction found. Two-developer sequence supersedes historical solo tickets. F00–F02 provides contracts and a minimal page; B01/B02 remain unimplemented. Imported storyboard is visual reference only and is never served/imported by the app.
- npm workspaces, one lockfile, exact direct package versions, Node 24.21.0/npm 11.19.0. Installed Node locally with nvm; no default shell runtime was intentionally changed. Node 23.3.0 was the original shell default and is unsuitable for this baseline. [Node release schedule](https://github.com/nodejs/Release) identifies 24 as LTS; [Vite requirements](https://vite.dev/guide/) are compatible. npm registry metadata and actual install/checks establish package compatibility. TypeScript 6.0.3 matches typescript-eslint's <6.1 peer range; did not adopt incompatible TypeScript 7. Tools are pinned, not claimed future-proof.
- 60-minute slots are valid wire data solely to represent the explicit duration-edit/reconfirmation scenario; this does not extend initial 30-minute domain availability. Public blocked state maps to NO_AGREEMENT; no public feasibility reason or private recipient is sent.
- A owns root config/lockfile/CI; B owns contracts after bootstrap with A compatibility review. Backend folders are reserved workspaces, not independent deployed services. Contracts hold schemas and hash helpers, no fixture values. Synthetic private examples stay in test-support; public-only examples live beside the UI mock adapter.
- Material risks remain: backend identity/projection enforcement, transaction races, idempotency, grant expiry and audience checks are not implemented. Public outcomes can reveal information by inference. No cryptographic private computation or zero-leakage claim. Mocked identities are not authentication.
- CI is a local-reviewable definition only; no remote CI run, branch protection or teammate review is claimed. No AWS resources, live model calls, Alexa host integration, repository publishing or money spent. Current hackathon eligibility/access/rules must be rechecked at release; dated research was imported for reference and is not current compliance evidence.

- User update, September 19: every task now starts with Astra as working lead and explicit authority to choose available local subagents/model effort. See [agent workflow](agent-workflow.md). This supersedes the imported plan's suggested agent allocation only; preserves two human owners, task boundaries, same-account execution and human review. All 18 task files and both handoffs link the policy. No account configuration, task status, implementation scope or immutable reference was changed.

- User update, September 20: [current workflow](agent-workflow.md) and [task board](task-board.md) supersede older execution/model/branch/ownership/sequencing rules, including the September 19 Astra-led policy. Use named direct workers, main in separate clones, independent frontend preparation plus separate live integration, and bounded Astra checkpoints. B01 is reported done and B02 active in B's clone (not verified here); preserve both. B may claim eligible A tasks or explicitly released work when A lacks tokens under the recorded transfer procedure. No publishing or Git integration is authorized by this documentation change. Imported product/security semantics and reference bytes remain unchanged.

- B01, September 20: the accepted foundation is `ca9fb636974030bfd8a620cec2b3d8581b3c8114`. The pure solver uses an internal owner `availabilityReview` bound to the context/input revision to distinguish confirmed hard exclusions from unknown coverage. Full meeting and assigned-duty intervals require reviewed coverage; the union of positive HARD_AVAILABILITY intervals defines the immutable envelope, and negotiable conditions further restrict it. Nina's synthetic hard envelope includes 11:00; her separate negotiable condition blocks it until the scoped exception. This avoids using exceptions to override hard conditions. B02 must obtain review coverage from explicit owner confirmation, not automatically infer it from the schedule. Wire contracts are unchanged. The direct Node demo required coordinated existing-workspace lock entries and TypeScript extension support under noEmit; no new external dependencies. B01 remains REVIEW with actual evidence in verification.md; application authorization, public projections and transactions remain B02 work.

- Main consolidation: the user authorized integrating A01/workflow and available B tasks onto main. B01 source commit is `04c87d7`; B02 is confirmed still in progress in B’s clone. Keep the current execution policy, retain both sets of historical evidence, and coordinate B01’s existing workspace/TypeScript changes without altering wire contracts. See [integration record](main-integration.md).
